{
  "buildCommand": "cd packages/player-vue && pnpm install && pnpm run build",
  "outputDirectory": "packages/player-vue/dist",
  "installCommand": "npm install -g pnpm && pnpm install",
  "framework": "vite"
}
