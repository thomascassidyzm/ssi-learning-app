# WORKLIST — ssi-learning-app · shared, multi-agent. READ THIS HEADER BEFORE EDITING.

The live "what's next" for this repo, for **all** agents (local, cloud/web, any account).
Coarse on purpose — for Opus: **directions, things to build, areas to think through.** One line per
item; if it needs detail, **link a doc** (the plans under `docs/methodology/`), don't inline it.
This is *not* a bug tracker or a subtask list, and it sits **on top of** the dev-branch rail in
`CLAUDE.md` — it doesn't restate it. Per-doc status lives in `docs/methodology/README.md`.

### How to use (the whole protocol)

**Status marks** — one box + a suffix, nothing else:
- `[ ]` open — free to grab
- `[~] @handle MM-DD` — claimed / in progress (e.g. `[~] @tom-local 06-13`)
- `[x] @handle MM-DD` — done (leave it; the groomer archives it)
- `[!] @handle MM-DD — why` — blocked / parked

`@handle` = a stable tag you pick for yourself (`@tom-local`, `@cloud-3`, `@web-acctB`). Date = today, `MM-DD`.

- **Grab:** flip `[ ]`→`[~] @you MM-DD` and commit **only that one line** (`worklist: claim <slug>`). If it's already `[~]`, pick another. A `[~]` older than **5 days** with no branch behind it is stale — re-grab it, note `(was @x, stale)`.
- **Add:** append `[ ]` to the **end** of the relevant section. Never renumber/reorder/reflow existing lines (append-only keeps merges trivial).
- **Finish:** flip `[~]`→`[x] @you MM-DD`, same one-line commit.
- **Merge conflicts here are always two independent line edits → keep BOTH, strip the markers.** Never overwrite the other side. Don't bundle worklist edits with code commits.
- **Branch hygiene** (inherits `CLAUDE.md`): claim/finish commits ride your normal working branch. **Never** add a worklist commit onto someone else's `claude/**` branch (it auto-merges wholesale to dev).

---

## 🧭 Directions / bets   (the why — changes rarely)

- **📋 THE consolidated build plan** (paywall ✅ shipped-to-dev · schools billing · downloads+handshake · rollout · loose ends) → `docs/methodology/build-plan-2026-06-16.md` — single pickup-point for whats next.

- **The Insight Engine is the spine.** Every new analytics surface is an *instance of the engine* (a registry metric + a library widget + a `discover`/`explain` call), never a one-off page. → `docs/methodology/insight-engine.md`
- **Measure speaking without ASR.** Behavioural tier now (taps, skips, latency, calibration); prosody/VAD later, adoption-paced. → `docs/methodology/metrics-architecture.md`
- **One substrate, many lenses: `learner → class → group → school → chain`.** Same metrics rolled up a level. The **class is a first-class learner-equivalent** (gets the learner self-view), read by its teacher and the leaders above. → `docs/methodology/tutor-insights.md`
- **Tags & relationships, not folders & ownership** — but a real *belonging* stays a hard FK (a class belongs to a school). Grouping above the class is flexible overlapping tags.
- **Database-first; quality bar for schools is zero-tolerance** (no audio that mismatches text). Promotion is `dev → staging → main`.
- **Every decision passes the BSC test — Better × Simpler × Cheaper (multiplicative).** Write the three-bullet narrative or don't do it. Agents self-apply it and proceed at >90% narrative confidence (no need to ask Tom). → `CLAUDE.md` "Decision heuristic: the BSC test"


- 🧭 **ALL-LANGUAGES ON PAID (Tom, 2026-07-14): the cross-offer is dissolved — "if a school pays, it gets access to all languages."** One paying teacher seat (£15/mo per-teacher PAYG, per the settled group commercial model) unlocks EVERY language for that teacher's class play — French Monday, German Tuesday, Chinese Wednesday, same class. Rationale: our marginal cost is serve-bandwidth only; 10 hours of one language costs the same as 1 hour of ten. Implementation implication: NO per-course entitlement state needed for paid seats — the check is "active paid seat → all courses"; free tier keeps free/community languages + the Welsh year. This SIMPLIFIES the trial/entitlement model — delete per-course blocking for paid, don't extend it.
- 🧭 **ANALYTICS ARE REAL OR ABSENT (Tom, 2026-07-14).** Customer-visible analytics must show real data from the Insight Engine (the spine, per the standing direction above) — never seeded demo presented as live. Demo mode is an explicit toggle only. Corollary: comparison/benchmark views (school-vs-country, student-vs-class) only make sense once real cohorts exist — wire them real (AnalyticsView benchmark loose-end above) but don't fake them meanwhile; empty-with-honesty beats seeded.

## 🔨 To build   (claimable — one line, link the plan)

- [x] @claude-local 06-13 **Metrics rollup (workstream A → B4) — SHIPPED** (`f9929aaf`): the per-(learner,lego) difficulty *series* B1/B4 read is now persisted (Option 2 via BSC — series in Layer 1). `LegoMetricsStore.upsertSeries` + a bounded ring in `useAdaptationEngine` (seeded on init → carries across sessions), written in an **isolated upsert** so it can't regress mastery. core 416 + player-vue 344 + typecheck + build green. **ACTIVATED 06-14** (migration applied — the series now persists live; **M0 complete**). Next: M1 consumers read the live series (teacher 'needs attention' + curvature overlay, D3).
- [ ] **Class-as-first-class, in order:** ~~migration~~ ✅ → ~~relationship reads~~ ✅ (`90dd857e`) → ~~tag-write endpoint~~ ✅ (`31b35312`) → **RLS rebase** (last co-teaching gate, below) → **coverage boards**. Co-teaching is now wired end-to-end except a non-lead teacher can't yet SELECT a co-taught class until the RLS swaps ownership→membership. → `docs/methodology/class-first-class-citizen.md` (rollout §6)
- [x] @web 06-14 **Coverage boards (class-as-learner) — SHIPPED demo-first (`055041f3`).** `CoverageBoard.vue` (5th Insight board) + `coverage` metric/resolver + `demoCoverage()` + draft RPC `analytics_class_coverage` (group-by over existing `class_sessions`, NOT applied — Tom applies). Renders now via `?demo`; real path gated only on the RPC. Built ahead of the class-first-citizen migration because coverage groups by `class_id` (doesn't need it). Pace/dosage/efficiency over wall-clock. Per-teacher scoping is the follow-on (flexible-grouping). → `tutor-insights.md` §2
- [x] @claude-local 06-14 **Teacher-tag write endpoint — SHIPPED** (`31b35312`): `POST /api/teacher/class-teachers` (add/remove/hand-over, service-role, authz = class teacher / platform / school admin) + `addClassTeacher`/`removeClassTeacher` client helpers; `createClass` now seeds the creator's relationship (closes the lead-pointer-only gap). The **RLS rebase below is the only remaining piece** for a non-lead teacher to SELECT a co-taught class.
- [x] **RLS rebase — CANCELLED 06-14 (Tom's call), replaced by server-mediated teacher reads.** The 4-agent assessment showed RLS-on-the-schools-surface is *avoidable*: route teacher *reads* through service-role endpoints (membership authz in code) like the *writes* already are, and the schools tables need no teacher-RLS branch — RLS stays only for the player's own-row learner-data (already on). Removes the delicate live-RLS surgery entirely. See `class-first-class-citizen.md` §5 (SUPERSEDED banner).
- [ ] **Schools teacher reads → server-mediated (rides co-teaching build).** Move the schools dashboard's class/student/teacher *reads* (`useClassesData`/`useTeachersData`/`useStudentsData`/`useAnalyticsData`) behind service-role endpoints (membership authz), replacing the client-direct `class_teachers` reads. **No live consumer yet** (no add-teacher UI, 0 co-teachers), so build this when co-teaching is actually built — not before (earn-it). The shipped client-direct membership reads work for lead teachers in the meantime.
- [ ] **Insight Engine boards beyond the Discovery feed** — course scoreboard, content-friction queue, health strip. → `docs/methodology/insight-engine-build-plan.md`
- [ ] **Atom-fusion upstream (Popty):** persist the 3 files + atom map and forced-align the clause once; the compute core already landed on dev. → `docs/atom-fusion-introduction.md`
- [ ] **Forced-alignment path** (remove the Azure-timings dependency; covers Welsh-human + xAI-no-timings). Re-validation in progress.
- [ ] **Schools loose ends:** bulk-invite-staff endpoint (`SetupView.vue` TODO); wire school/global benchmarks (`AnalyticsView.vue:290` shows class-avg only); verify `contentFriction` RPC migration `20260602` is applied.
- [ ] **Schools-offering audit follow-ups (logged 07-02, from the 4-agent review; the criticals/highs are FIXED on dev — see `4a904dca`…`4ead670c`):**
  - Webhook ordering-safety: platform handlers absolute-SET from whatever event arrives; a delayed stale event can overwrite newer state until the next event heals it. Fix = compare `occurredAt` against a stored `platform_updated_at` (needs a column → migration).
  - `teacher_seats` is display-only: nothing gates a new teacher joining past the paid seat count, nothing decrements on remove — drift = under-billing. Needs a policy call (hard cap vs nudge) before code. **DECIDED 07-02 (no gating, ever — per class/per teacher/at all): make the display honest instead.** `[x] @claude-local 07-03` — `UpgradeView.vue` now fetches the real joined-teacher count (`useTeachersData`) and shows it next to paid seats, flagging over-joined schools. → `49b955df`
  - SCHOOL `past_due` = instant dashboard lockout (tutor lane now has dunning grace via `teacher_paid`; schools still lock the moment Paddle marks past_due). Policy call: grace window while retries run? **DECIDED 07-02: yes, mirror the tutor lane.** `[x] @claude-local 07-03` — `past_due` now retains access (server `api/school/subscription.ts` + client `useSchoolContext.platformActive`), with a `platformPastDue` banner for school admins; only terminal expired/cancelled locks. → `9af72db3`
  - `subscriptions` is one-row-per-learner (`UNIQUE learner_id`) and all three purchase kinds upsert it, overwriting `plan_name` — a tutor who also buys a student sub clobbers their tutor-bundle row. Design constraint to revisit before tutors commonly hold two subscriptions. **DECIDED 07-02: precedence guard now, defer multi-row schema redesign.** `[x] @claude-local 07-03` — shared `wouldDowngradePlan`/`PLAN_PRECEDENCE` guard (tutor bundle > premium > student access) at all 3 upsert sites in `api/teacher/paddle-webhook.ts`; no schema change. → `a9b14fb4`
  - Onboarding cold-load flash hypothesis (unverified): `showNoAccess` may flash before the async role fetch resolves on first-ever load.
- [x] @web 06-13 **Curvature engine (metrics B1)** — level/velocity/acceleration via trailing local quadratic fit + own-noise alarm; pure `@ssi/core` `learning/curvature.ts` (17 tests, APML spec). The start-now compute primitive; consumers B4/C2/D3 gated on it. → `docs/methodology/metrics-implementation-plan.md` §1 B1
- [x] @web 06-13 **Competence band (metrics G1)** — flat hours-on-task estimate (30h/100h anchors) in pure `@ssi/core` `learning/competenceBand.ts` (10 tests, APML). Reads `total_practice_minutes` (exists today). Compute only; admin/tutor surface + legal framing copy left for Tom/Aran. → `metrics-implementation-plan.md` §1 G1 / `metrics-architecture.md` §12 H2
- [x] @web 06-13 **Local difficulty sensing (metrics B4)** — curvature per (learner, unit) in pure `@ssi/core` `learning/localDifficulty.ts` (9 tests, APML). Sensing only (struggling/easing/steady); the bridge from B1 to the M2 controller. Live wiring needs persisted Layer-1 (A3 migration). → `metrics-implementation-plan.md` §1 B4
- [x] @web 06-14 **Synthetic telemetry for demo learners — SHIPPED (`895f6cd3`).** `generate-demo-suite.cjs` now populates `learner_lego_metrics` (incl. `recent_latency_samples` series + Layer-1 scalars) + a bounded set of `player_events` for the `is_demo` learners, with calibrated struggling/easing/steady curve shapes, so the difficulty-turns board + curvature engine light up on *named* demo learners on the real path. Pre-flight gates on the column existing so it's safe pre-migration. **Requires migrations 20260613 (a3 + latency series) + 20260614 applied, then re-run the generator (also now needs `pnpm --filter @ssi/core build` first).** → `tutor-insights.md` §3
- [x] @web 06-14 **Lock the demo curve calibration — SHIPPED (`bdfc653a`).** Canonical tested `@ssi/core` `learning/syntheticSeries.ts` `makeLatencySeries(kind,{rng})` + regression test asserting classification through the REAL sensor at its production defaults (struggling/easing 100%, steady ≥85% — honest floor, see commit). Generator imports it from the built dist (single source of truth; magic numbers deleted). core 422 green.
- [x] @claude-local 06-14 **Insight-RPC fixes (from the overnight-work review) — APPLIED 06-15 (verified: all 3 RPCs live + gated; coverage went absent→live).** (1) rewrote the draft `20260614_analytics_class_coverage.sql`: `legos_advanced` is now a TRUE lego count via a `course_legos` ordinal (was the packed `seed*100+lego` key used as a distance → ~100× inflated pace/efficiency; **verified live 200→5**, missing/stale ids floor to 0), + bakes in the demo-school (`is_demo`) exclusion. (2) new `20260614_insight_rpc_demo_exclusion.sql`: re-creates the ALREADY-APPLIED `analytics_difficulty_turns` + `analytics_health` with a NULL-safe `is_demo` exclusion. ⚠️ **DO NOT run `generate-demo-suite.cjs` against prod until (2) is applied** — else 177 demo learners surface as real on `/admin/insights` and deflate the audio-failure safety metric. After apply the real coverage board reads empty until a real school logs sessions (correct; `?demo` still renders client-side). **Migration files COMMITTED to the repo 06-15** (were applied-but-uncommitted).
- [x] @claude-local 06-15 **Insight "Rate compare" widget + Teacher/Tutor view — SHIPPED demo-first to dev** (`22fc36be`→`f6eef4f4`). The reusable **entity-vs-average RATE** widget: pick a metric, an entity, and a comparison average — all swappable — one widget for every rate (6 hero rates: progress pace · app-min/day · new-LEGOs/hr · repeats · interactions/min · days-active/wk). Two surfaces: ADMIN `/admin/insights` "Rate compare" tab (`RatesBoard.vue`, browse any entity) + the TEACHER view **`/teacher-insights?demo`** (`TeacherInsightsView.vue` — calm single-purpose page, scoped to the teacher's OWN classes via a "Your classes" picker, nothing but the widget). **PRIVACY:** entity-vs-AGGREGATE only, NEVER another named entity — the cohort renders an anonymised distribution (only "You" + the average + percentile). **DESIGN** (frontend-design skill + Apple-HIG liquid glass): green/blue (blue=you, green=positive), glass on the CHROME only (controls/header) with the content card clean, a soft green/blue atmosphere behind for the glass to refract, edging, reduced-transparency/motion fallbacks; headline = two-number **`entity v average`** stat (entity is the hero in a glassy outlined lozenge), mirroring the in-app community stat. 363 player-vue tests green. Components: `insight/components/RateCompare.vue` + `RateTrend.vue`, `insight/data/demoRates.ts`. **DEMO-ONLY (fixtures); live-rate wiring DEFERRED — no schools onboarded yet** (earn-it: wire when the waiting schools go live; engagement/coverage rates are real+populated, the latency/execution lane is not). → [[project_ssi_insight_rate_compare]]
- [x] @claude-local 06-15 **Free-access flow: email ALLOWLIST + FORGIVING codes — SHIPPED to dev, verified live, migration applied** (`34e8edfb` + `1587c585`). Root cause of users' "invalid code": format/separator sensitivity (validation was only `trim().toUpperCase()`). (1) **Forgiving codes** — stored generated `code_normalized` (upper + strip non-alnum) on `entitlement_codes`/`invite_codes`, exposed via the validation views; `validate`/`redeem` match the normalized form → `zvk078`/`ZVK 078`/`zvk-078` all resolve to `ZVK-078` (verified end-to-end on the deployed endpoint). (2) **Email allowlist** — `email_access_grants` (RLS-on/no-policy, service-role only) + `POST /api/access/grant-emails` (admin-gated: paste emails → grants + best-effort immediate-apply to existing accounts via `verified_emails`) + `POST /api/access/claim` (every sign-in: applies pending grants for the **verified-session** email; idempotent via partial-unique `user_entitlements.email_access_grant_id`) + Allowlist panel in `AdminAccess.vue`. Pre-authorised emails get free access on sign-in, **no code**. Security-reviewed (email from session not body; admin gate; service-role-only table; idempotent). Migration `20260615_access_allowlist_and_forgiving_codes.sql` APPLIED (first attempt hit a `CREATE OR REPLACE VIEW` column-reorder error → fixed by appending `code_normalized` last). Shared DB → schema live for prod too (additive, prod unaffected); app code reaches users via dev→staging→main. → [[reference_ssi_free_access_flow]]
- [~] @claude-local 06-16 **Layer-1 listening → 30-cup model** (replaces the interval-50 / 80-20 / 2×-pass `useLayer1Scheduler`). 30-cup wheel, one cup/round; fill +1 seed/cup per 30 *introduced* seeds, cap 600→20/cup; universal decay ladder `1×2×→2×2×→2×` (clusters enter at 2nd rung); target-only; forever-loop rests at floor. **Phase 0 SHIPPED to dev** (`08e4a4b5`): engine + LearningPlayer wiring (fires every clean non-pod boundary) with a deterministic fallback grouping; 30 scheduler + 418 player-vue tests + typecheck green; test via `?l1test`. **Phase 1 (pending Aran)** = `course_listening_clusters` table + loader + authored linguistic templates (5/10/15/20). **Phase 2** = every-round bookend treatment. → `docs/methodology/layer1-listening-cups.md`
- [ ] **Playback write-path DB contention (found 07-02 during slow-load hunt):** `bump_speaking_opportunities` averages 1.2s/call and `record_lego_pairings` 0.7s (pg_stat_statements) — trivial upserts on tiny indexed tables, so almost certainly same-row lock queueing from concurrent flushes (pause + visibility-hidden + unload). Not on the load path, but bursts can stall the shared pooler for everyone. Diagnose server-side (EXPLAIN under concurrency), then either serialise client flushes or batch. Also: `useEagerScriptPreload` is now inert plumbing (nothing calls preload since 989ff752) — delete it + its LearningPlayer/DemoLauncher consumers on a quiet day.
- [x] @claude-local 07-03 **MONEY: guest belt-picker bypasses the Yellow-belt trial wall — VERIFIED ALREADY FIXED (`a9e96643`) + one stretch nit landed (`5a799d2e`).** Traced every belt-jump entry point (central-pill `handleSkipToBelt`, `CourseBrowser`'s `ssi-jump-to-seed`, deep-link `?seed=`): all three already route through `gateSeed`→`canAccessSeed` as of `655bdf203` (06-16), already on dev/staging/main — the picker does NOT currently bypass the wall. Added a regression test at the shared gate (`packages/core/src/pricing/access.test.ts`) since none existed: a guest jumping straight to Orange belt (past the Yellow-preview ceiling) on a premium course is blocked. Server-side strict entitlement (e95bb611 scaffold) remains the belt-and-braces follow-up. Nits: fixed `demo:selectCourse`'s undefined-target_lang crash (derives target/known lang from the course_code convention when the dispatched stub omits them). NOT done: welcome-audio uuid double-fetch — skipped, that area is under active first-boot redesign (see `bc949c0e`/`a64c2e71`) and isn't a quick, low-risk touch right now.
- [ ] **Schools teacher-7d + daily-activity rollup (from the 07-04 Fable review repoint).** StudentProgressView + fetchCourseStats already repointed to player_events rollups (`a37c533e`); TeacherDashboard "Hours/wk" relabelled honestly as all-time (`299dc3f2`). Still legacy-sourced / needs a NEW cross-learner rollup over `learner_speaking_opportunities`: (a) per-class 7-day practice window for TeacherDashboard, (b) per-day school/group activity series for `useAnalyticsData.fetchDailyActivity`. Blocked on an **authz-pattern call**: server-mediated `api/` endpoint with membership check (matches the "teacher reads → server-mediated" direction) vs a client `SECURITY DEFINER` RPC. Recommend the endpoint.
- [ ] **AudioCache eviction (unbounded ephemeral growth) — DESIGN written, awaits go.** Ephemeral (played) audio never evicts; `evictToTarget` unused. Proposal grounds Tom's "evict ephemeral >100 LEGOs behind position, keep offline-downloaded, re-fetch from S3 on demand" in the existing `ephemeralOwnerLegoId` + `by-ephemeral-owner` index. Folds in two cache tangles (clearCourse `courseCode:null` no-op; content-version invalidation clears the wrong/broad cache). → `docs/audio-cache-eviction-design.md` (open questions: trigger cadence, KEEP_BEHIND).
- [ ] **`lego_progress` UNIQUE(learner_id, lego_id) omits course_id (07-04 review).** Lego ids repeat across courses, so a multi-course learner cross-contaminates progress (and course-B inserts hit the constraint and are swallowed). Fix = add `course_id` to the unique key + `getLegoProgressById`'s filter (same for `seed_progress`). Migration parked at `supabase/migrations/20260704_course_scope_progress_unique.sql` — its REAL gate is "the `getLegoProgressById` course_id read fix (`1b92ba6a`) live on MAIN" (shared DB, expand-contract), not the RLS window. Apply via canary once the next dev→staging→main promotion lands.
- [ ] **Revoke authenticated SELECT on `invite_codes` — gated on `api/admin/codes.ts` reaching MAIN.** Code strings are bearer credentials (join codes redeem into privilege grants), so any signed-in user can currently enumerate them. AdminAccess.vue repointed to the new endpoint (07-04; also un-breaks the is_active toggle, which the June grant revokes had silently blocked). Migration parked at `supabase/migrations/20260704_gated_invite_codes_select_revoke.sql`. Context: same-day grant hygiene went LIVE on the shared DB (classes DELETE, govt_admins DELETE/TRUNCATE, entitlement_grants anon SELECT revoked — 14/14 via `supabase/secfix-toolkit/canary_grant_hygiene.cjs`); privileged bearer codes bounded (`20260704_bound_privileged_codes.sql`); CLAUDE.md RLS-window trigger rewritten condition-based + standing doctrine added.
- [x] @claude-local 07-13 **Region tier slice 1: group_id wiring at redemption — SHIPPED (`9e4f3542`, migration fix `48c057c4`).** Per `docs/schools/region-tier-design.md` (branch `schools/region-tier-design`) §6 slice 1: `redeem.ts` govt_admin branch honours `grants_group_id`/creates the group at redemption when absent; school_admin branch stamps `group_id` at birth, registers BOTH join codes (was teacher-only — silent admin-code bug fixed), routes through `/schools1` so `onboarding/provision` sets platform trial clocks (previously skipped for invite-created schools); `validate.ts` prefers group-name over the legacy regions lookup; `create-govt-admin.ts` drops the synthetic learner/govt_admins pre-creation, `group_id` now optional. `region_code`→`group_id` backfill (dry-run default) + read-only mismatch audit at `scripts/region-tier/` (design §2 steps 1-2 only — dual-read stays, NOT applied against real data yet). New `api/_utils/schoolJoinCodes.ts` shared helper. Tests: `api/code/redeem.test.ts` (4 cases); full suites green (api 13/13, player-vue 562/562, both typechecks clean). **Next: slice 2** (self-serve region naming + group-bound school links) **and slice 3** (adoption flow + consent toggle) per the design.
- [x] @claude-local 07-13 **Region tier slice 2: leader surfaces — SHIPPED (`fc502ee8`, merged to dev).** Per `docs/schools/region-tier-design.md` §6 slice 2 + owner's REVISED §5c ruling: `PATCH /api/groups/:id` name-only leader self-rename (server-derived ownership, type/parent_id stay ssi_admin-only); `invite/create.ts` stamps `grants_group_id` for school_admin codes from the caller's own `govt_admins` row (never the client — cross-region minting now impossible); new `POST /api/govt/create-school` — leader creates a school group-owned from birth with a VACANT admin seat (`admin_user_id` NULL, verified safe — every read is an equality check), both join codes registered, 365-day trial clock set at creation (this claim path never routes through onboarding/provision); new `GET /api/govt/school-links` — leader's minted links + redemption state, scoped to their own group. Migration: `schools.admin_user_id` nullable, `groups.name_confirmed` (backfilled true existing / defaults false new) drives the DashboardView "name your region" first-run card + new "Schools in your region" mint/list/create surface (`useGovtAdminActions` composable). Audit + backfill scripts run read-only/dry-run against production (design §2 steps 1-2, NOT applied): 1 pre-existing region/group mismatch (Ireland), 2 groupless region_codes (japan, wales) — logged, no writes. Tests: 4 new files/17 cases (wrong-group/no-group rejection paths); full suites green (api 30/30, player-vue 562/562, both typechecks clean). **Next: slice 3** (adoption flow + consent toggle) per the design.
- [x] @claude-local 07-13 **Region tier slice 2 addendum — SHIPPED (`75689d3c`, merged to dev).** Owner ruling mid-flight: user-facing copy is GROUP everywhere, never region (schema/`govt_admins`/`region_code` untouched — copy + routes only). New `/group/:code?` landing door (also `?code=` query) — a signup-page-skinned entry to the existing govt_admin redemption flow, matching the `/schools1`/`/tutors` convention; `RedeemCode.vue` gained a presentation-only `variant` prop (`'bare'` default / `'landing'`) reusing the same validate/OTP/redeem machinery, just a hero+checklist instead of a bare card; `validate.ts` now surfaces `organization_name` as `groupName` pre-group-creation so the hero has copy before the group exists. Confirmed no slice-2 code assumes single-level hierarchy (create-school/school-links scope to the caller's own `group_id`; subtree queries already handle arbitrary depth) per the future-proofing note. Verified live via dev-server screenshot. Full suites green (api 30/30, player-vue 562/562, both typechecks clean).
- [x] @claude-local 07-13 **Region tier E2E journey — staging robot test, harness SHIPPED (`8cafc122`, merged to dev), one real bug found.** `packages/player-vue/e2e/region-tier/` (Playwright, `pnpm e2e:region-tier`, `PROD_URL=https://staging.saysomethingin.app`): full leader → group → school journey against real staging with synthetic `@ssi-internal.test` accounts, OTP-minted via `auth.admin.generateLink`'s `email_otp` (zero real email sent). Confirmed server-side correct: govt_admin group-creation-at-redemption, group rename, school-link mint + `grants_group_id` stamping, direct school creation (vacant admin seat), and — the important one — a forged `grants_group_id`/`group_id` payload on `/api/invite/create` and `/api/govt/create-school` is always overridden by the caller's own `govt_admins.group_id` (cross-region minting confirmed impossible, both API-level and DB-verified). **Break found (not fixed — reported for a fix worker):** a freshly-redeemed govt_admin leader visiting `/schools` for the first time is bounced to `/` (the learner player) instead of the dashboard — router `beforeEnter`'s `hasSchoolRole` check reads `false` despite `RedeemCode.vue` calling `useUserRole().initialize(null,'govt_admin')` synchronously before redirect; root cause not isolated further (candidate: `ssi-user-role` localStorage cache not durably read back on the very next full-reload navigation). Blocks the leader dashboard UI (name-group card, mint-links UI, create-school UI) until a subsequent reload/navigation — does not affect data correctness. Full report + screenshots in the session transcript.
- [x] @claude-local 07-13 **First-visit `/schools` bounce after govt_admin redemption — FIXED (`5f7c1475`, merged to dev `90e2ec45`).** Root cause: a race between `RedeemCode.vue`'s optimistic post-redemption role write and `useAuth`'s SIGNED_IN-listener-triggered `ensureLearnerExists()` re-sync — the latter does an extra `verified_emails` round trip, so it can resolve LATER with pre-redemption (stale) role data and clobber the correct write before the `/schools` guard runs; a later reload works because by then everything has settled. Fix: `useAuth.refreshRole()` (re-runs `ensureLearnerExists()`) is now awaited in `RedeemCode.doRedeem()` right after the optimistic role write and before marking `success`/redirecting — since it's issued after the redemption's DB write commits, it always reads the correct row, making it the authoritative last write regardless of listener timing. Added 4 router-guard unit tests (`schoolsGuard.test.ts`, real navigation via the actual router). Verified against a local vite dev server proxying `/api` to staging (shared DB) with the region-tier E2E suite (vercel CLI deploy unavailable in-sandbox — SSL errors on the upload endpoint): previously-cascading steps 2d ("name your group" card), 2e, 3a-3c, 5a, 5c now pass (were all failing/cascaded from the bounce). **New bug found + logged separately, NOT fixed here (different root cause, out of scope):** the school_admin redemption path double/triple-redeems (`doRedeem()` fires from both `handleVerifyOtp` directly and the `watch(isSignedIn)` side-effect), creating multiple `schools` rows per admin — see next item.
- [x] @claude-local 07-13 **School_admin invite redemption double-redeem race — FIXED.** Root cause confirmed: `RedeemCode.vue`'s two `doRedeem()` call sites (direct + the `isSignedIn` watcher) really do race — Supabase's `onAuthStateChange` can fire before `verifyOtp()`'s own promise resolves — and `redeemCode()` had no guard, so both could POST `/api/code/redeem` before either cleared `pendingCode`. Client fix: `useInviteCode.redeemCode()` single-flights via a memoized in-flight promise (both call sites now safely coexist). Server fix (the layer that actually matters): migration `20260713_school_admin_unique_natural_key.sql` adds unique indexes on `schools.admin_user_id` (partial, non-null) and `govt_admins.user_id` — matching what every read site already assumed — applied live (canary-checked: zero pre-existing duplicates, committed); `api/code/redeem.ts`'s school_admin and govt_admin branches now select-then-insert and gracefully reuse the winner's row on `23505`, plus clean up an orphaned group if the govt_admin race is lost. Swept staging/prod (`supabase/secfix-toolkit/sweep_duplicate_schools_2026-07-13.cjs`, read-only): **zero duplicate schools/govt_admins rows found** — Tom's manual staging cleanup was the only occurrence. Tests: 2 new concurrency tests (`api/code/redeem.test.ts` — school_admin and govt_admin races both converge on one row) + 2 client single-flight tests (`useInviteCode.test.ts`); full suites green (497 core + 568 player-vue + 32 api) and both typechecks clean.
- [x] @claude-local 07-13 **ssi_admin surfaces audit for region-tier — SHIPPED (`95d8035d`, merged to dev `9c325377`), no deprecated views found.** Full inventory of every ssi_admin schools/groups/govt/invite surface against `docs/schools/region-tier-design.md` slices 1-3: `/admin/schools/:id` and `/admin/groups/:id` read-views (`AdminSchoolsContainer`/`AdminGroupContainer` + `useSchoolContext.loadFromSchoolId/loadFromGroupId`) already resolve generically via `group_id`/path-prefix, so schools born through either the redemption path or leader-direct-creation render correctly with zero changes needed — classified CURRENT, verified not touched. Two real bugs found and fixed in `SchoolsSetup.vue`'s "Add govt admin" panel (the surface the design doc names as Tom's actual entry point): (1) submit button required `newGovtGroup` truthy, silently blocking the group-optional "leader names their own group" flow the placeholder itself advertised; (2) result showed a bare invite code instead of the shareable `/group/:code` link (link-first doctrine) — both fixed, plus region→group wording. Same link-first fix applied to `AdminAccess.vue` (the generic invite/entitlement console, a second surface that mints the same govt_admin codes) — row copy buttons + success message now output `/group/:code` (govt_admin) or `/redeem/:code` (everything else) instead of bare codes. Deleted dead code found in the same pass: `SchoolsSetup.vue`'s `regions` table fetch/ref (fetched, never rendered — the UI already uses `groups` for the dropdown) and `schools.region_code` column (selected, never displayed); `AdminAccess.vue`'s unused `grants_region` field (typed, never read). Full suites green (player-vue 568/568, typecheck clean); no api/ changes so api suite untouched.
- [x] @claude-local 07-04 **Cursor-only position, phase 1: repoint client consumers — SHIPPED.** Per the 2026-07-04 cursor-only decision (`docs/position-and-ownership-model.md`), repointed every client read of `highest_completed_*`/`current_mode` onto the cursor (`last_completed_lego_id`) or a derived predicate: `hasReachedInfinitePlay` extracted to `utils/infinitePlay.ts` (unit-tested) and driven off the cursor everywhere (resolveStartLegoId, instant-playback mode pre-check, mid-session resume check); resume's ceiling fallback removed (cursor → R1, nothing else); `useBeltProgress` mergeProgress/highestBeltIndex/highestLegoId no longer ratchet — they mirror the cursor, so belt-complete ticks (CourseBrowser, PlayerContainer, SettingsScreen) follow it down too; course-list cards (App/BrowseScreen/CourseSelector) and admin (`useAdminUserDetail`/`AdminUserDetail.vue`) now prefer the cursor. NO schema change — legacy fields still written for stale-PWA compat. 473 player-vue + 460 core tests green; typecheck clean (pre-existing `useLearningSession.ts:499` failure only). Phase 2 (deferred, needs DB-side work): repoint `analytics_learner_progress_rate` + the admin-stats-rebuild-brief SQL view design off the ceiling; decide whether to unfreeze the cursor during INF PLAY so `infplay_round_index` depth can derive from `round_index` instead of the counter; stop-writes + column/trigger drop timing.
- [x] @claude-local 07-13 **Invite-born school_admin: single-OTP flow — SHIPPED.** Fixes the second-OTP bug from tonight's soak: `redeem.ts`'s school_admin branch redirected invite-born admins through `/schools1` purely to trigger `POST /api/onboarding/provision`'s trial-clock write — but `Onboarding.vue` has no session awareness and always starts at its email step, forcing a second email+OTP. Moved that provisioning server-side: extracted `provisionSchoolPlatformTrial`/`provisionTutorPlatformTrial`/`burnTrial` out of `provision.ts` into shared `api/_utils/schoolPlatformTrial.ts` (schoolJoinCodes.ts extraction precedent); `redeem.ts`'s school_admin branch now calls it directly at redemption (no course chosen yet at invite time → grants the no-course-lock 1-year window, same generosity self-serve gives minority-language schools; `TeacherDashboard.vue` already reads a null `trial_course_code` as "no restriction") and redirects straight to `/schools`. Self-serve (`/schools1`/`/schools2`) is untouched — it never redeems a code, so this redirect change only fires post-redemption. New migration `20260713_school_name_confirmed.sql` (`schools.name_confirmed`, mirrors `groups.name_confirmed`) backs a "Confirm your school's name" first-run card on `DashboardView.vue` (same pattern as the leader's "name your group" card) — invite-born schools are pre-named from the inviting leader's guess but unconfirmed until the admin saves it once; self-serve schools default confirmed=true, no card. APML updated (`apml/schools/ssi-schools-integration.apml`, new `SingleAuthOnboarding` principle).
- [x] @claude-local 07-13 **Group_id lost in redeem→onboarding handoff — FIXED (`45a27499`).** Live repro on staging: a leader-minted school_admin invite redeemed by an admin whose school row lost the race (a concurrent ungrouped insert — e.g. a self-serve `/schools1` provision for the same admin — committed first) ended up permanently group-less, invisible on `/schools/all`. Root cause: the 4aef6b86 group-reattachment fix only ran for the precheck-existing branch (`existing` found BEFORE any insert attempt) — the sibling branch, where THIS request's own insert lost the `admin_user_id` unique-index race and re-read the winner's (ungrouped) row, fell straight through with no reattachment. `mintSchoolLink`/`invite/create.ts`'s `grants_group_id` derivation and `provision.ts`'s existing-school lookup were both verified correct — not the leak. Fix: moved the "attach invite's group_id if unset" check to run unconditionally on whatever `newSchool` was finally resolved to (precheck-existing, insert-won, or raced-reread), skipping only the freshly-inserted-by-this-request case (already correct). New regression test proves the old code was broken (fails without the fix, passes with it). Full suites green (580 player-vue + 39 api), both typechecks clean. **Data repair NOT run** (destructive DB write) — SQL for Tom to reattach the live orphan ('Gwynedd School 003') reported separately.
- [x] @claude-local 07-14 **Unify school creation — one primitive, awaiting-admin state — SHIPPED.** Collapsed the two leader-facing school-creation modes (mint-invite-then-birth-at-redemption vs. direct-create) into ONE: `POST /api/govt/create-school` (already built/tested pre-session) is now the ONLY leader surface, wired into `SchoolsView.vue` ("+ Add school", replacing "+ Onboard new school") and `DashboardView.vue`'s "Schools in your group" card (dropped the duplicate "Invite a school" button there too). Both create the school GROUP-OWNED FROM BIRTH with both join codes registered immediately — the row shows copyable admin/teacher `/redeem/:code` links forever, nothing pending. New migration `20260714_school_summary_claim_state.sql` (applied live + `schema.sql` refreshed) adds `teacher_join_code`/`admin_join_code`/`created_at`/`has_admin` to `school_summary` — `has_admin` is the honest claim signal (verified: `school_admin_join` redemption never sets `admin_user_id`, only an admin `user_tags` row — so admin_user_id-only would have under-reported "claimed" for every leader-born school). `/schools/all` row shows an "Awaiting admin" pill in place of the health dot until claimed; header lede counts it honestly. `SchoolsSetup.vue` (ssi_admin) got the same Awaiting/Claimed column via one extra `user_tags` query (kept the existing direct `schools` read rather than switching to the view — smaller diff). Old mint-invite path (`api/invite/create.ts` code_type=`school_admin`, `api/code/redeem.ts`'s `school_admin` branch) deliberately left alone — still redeems any outstanding pre-existing links, `DashboardView.vue` still lists them read-only. New tests: cross-group spoofed `group_id` is ignored (server always uses the caller's own `govt_admins.group_id`) + codes-registered-at-birth assertion, `api/govt/create-school.test.ts`. Full suites green (580 player-vue + 40 api), both typechecks clean. APML updated (`apml/schools/ssi-schools-integration.apml`, new `SchoolCreationOnePrimitive` principle).

- [ ] **NPTC gap: teacher class-wide skip/revisit** — per-learner jump exists, no class-level affordance; teachers hit this in week one of real use. Small-medium. (NPTC audit 2026-07-14, promise 9a.)
- [x] @claude-local 07-14 **NPTC gap: retire seeded-demo as the default on customer-visible teacher analytics — SHIPPED for the flagship metric.** TeacherInsightsView (serving all 3 schools roles, not just teachers — reused at `/schools/analytics`) now real-path-canonical for rate-of-progress (LEGOs/wk) at class-entity granularity: `useClassesData().fetchClasses()` (already role-scoped) replaces the hardcoded class fixture; `GET /api/school/rate-compare` (auth → `resolveVisibleScope` → new `analytics_class_sessions_scoped` RPC, service_role-only EXECUTE, applied+verified live) replaces `demoRates.ts`; compare-to = school/group/global average, k-floor 5 (matches `spec.ts`'s `Sovereignty.kFloor`), honest "not enough data" state below the floor — never a fabricated number. `?demo` unchanged (still the seeded preview). Deferred, not yet real: the other 5 HERO_RATES, the learner-level drill (needs the homework-sourced attention lane), a single school/group AGGREGATE entity (multi-course averaging isn't apples-to-apples — the roll-up instead lets any role pick any class in their scope). player-vue 586/586 + api 69/69 (16 new `rateCompare` math tests + 5 new endpoint tests + 48 pre-existing) + both typechecks green; Playwright-verified no console errors on `?demo` and the honest empty state on the real path. → `apml/interfaces/insight-engine.apml` (TEACHER_INSIGHTS_VIEW)
- [ ] **All-languages entitlement for paid seats** — implement Tom's 2026-07-14 ruling (direction above): active paid teacher seat → all courses playable as class; removes the one-trial-slot/one-course blocking for paid schools. Small once the ruling is applied as a simplification. HOLD: coordinate with the active signup-flows agent — same surfaces.
- [x] @claude-local 07-14 **Rate compare — course-first + full entity ladder — SHIPPED.** Owner's ruling: "choose per course as the top metric, then compare class/school/group v any other average — class v school, school v group, group v regional average, or global average." Course is now the normalising key: a "Course" picker leads `TeacherInsightsView` (options = courses with a class in the caller's scope); every aggregate below is computed within that one course. Full entity ladder, scope-checked and role-gated: teacher = class only; school_admin = class or their whole school; govt_admin = class, a school in their group, or their whole group — a role-gated segmented "Level" control. compare_to per level: class → school/group/global avg; school → group/global avg; group → region/global avg (region = sibling groups sharing the same `groups.parent_id`; honestly degrades to "no broader region yet" when the group has no parent — no fabricated cohort). K_FLOOR=5 held at EVERY level (peer classes, schools, OR groups). One RPC (`analytics_class_sessions_scoped`, unchanged) + one generalised math primitive (`aggregateWindowPace`/`aggregateWeeklyTrend`, `api/_utils/rateCompare.ts` — mean of member classes' window pace/trend; identical to the old per-class math for a one-member set) serves every level — no new RPC needed. `resolveVisibleScope` (`api/_utils/schoolScope.ts`) extended with `schoolIds`/`groupId` for school/group-level authorization. `GET /api/school/rate-compare` now takes `course_code`+`entity_level`+`entity_id`+`compare_to` (clean rename of `class_id`, both server+client updated together). `?demo` untouched. api 84/84 green (22 rateCompare math + 14 endpoint incl. school/group levels + 48 pre-existing), player-vue 586/586, both typechecks + build clean, Playwright-verified `?demo` unchanged with 0 console errors. Deferred: single-learner drill (unchanged, still demo-only), the other 5 HERO_RATES. → `apml/interfaces/insight-engine.apml` (TEACHER_INSIGHTS_VIEW)
- [x] @claude-local 07-14 **Rate compare — all-courses global cohort, alongside same-course — SHIPPED (same-day follow-up).** Owner's refinement: "relatedness determines the STRENGTH of a comparison, not its permission" — offer an "all SSi learners, all courses" global average as a comparison cohort at EVERY entity level, alongside (never replacing) the same-course cohorts from the item above. Added `compare_to=global_all_courses` to every level's option set (class/school/group all gain it, on top of their existing options). The entity's own value stays anchored to the selected course; only the cohort pool widens — `classIdsBySchool`/`subtreeClassIdsForGroupPath` now take a nullable `courseCode` (`null` = every course, ecosystem-wide), and each peer's rate is still self-computed on its own course before averaging, so mixing courses in the pool is mathematically sound (same "self-computed rate, then averaged" pattern as every other cohort here). Labelled explicitly everywhere so the weaker basis is visible at a glance: "Global average · this course" vs "Global average · all courses" (both the API response `average.label` and the `TeacherInsightsView` compare-to picker). Same K_FLOOR=5. Default/preferred `compare_to` per level is unchanged (still the most-related option — school/group/region) — the all-courses variant is always the last, opt-in choice, never the default. 4 new endpoint tests (cross-course inclusion at class level, k-floor held on the all-courses pool, peer-school aggregation across ALL its courses, group-level ancestor/descendant exclusion still holds with any-course peer classIds) + 1 existing label assertion updated. api 88/88 green, player-vue 586/586, both typechecks + build clean. → `apml/interfaces/insight-engine.apml` (TEACHER_INSIGHTS_VIEW)
- [x] @claude-local 07-14 **SEED playback simplified to t-k-t-t @1x (listening cups + drained-eternal speaking review) — SHIPPED.** Founders' decision (Tom + Aran): both Layer-1 listening cups (`useLayer1Scheduler.ts`) and the drained/eternal SEED-PHASE production review in SPEAKING mode (offset ≥144, `generateLearningScript.ts`) now play the SAME fixed sandwich — target → known → target → target, ALL @1×. Cups' `DEFAULT_SEED_PLAYLIST` dropped its trailing @2× stretch rep; `Layer1SlotRole`/`Layer1PlayRole` lost the speed-doubling members outright (deleted, not defaulted-away). The drained-seed review — previously the standard prompt/pause/voice1/voice2 production cycle — now emits the same sandwich as FOUR single-audio sub-cycles (new `emitSeedSandwich` helper, shared main-loop + INF PLAY), reusing the existing pod/listening single-audio-cycle trick (new `Cycle.singleAudio` flag in `@ssi/core`) rather than inventing a new phase machine. Also deleted the ENTIRE pre-cups Layer-1 decay/stage/urn-sampling machinery in `generateLearningScript.ts` + `useAlgorithmConfig.ts` (confirmed 100% dead since the 2026-05-19 L1 main-flow removal — `emitL1Cluster` was defined but never called) and its `useListeningProgress.ts`/`LegoListeningStore.ts` hydration wiring. **Explicitly UNCHANGED, per the decision: pod speaking-mode decay, Listening-mode pods (drill/immersion), Stage 0/fusion ladder.** core 497 + player-vue 589 tests green, both typechecks + core build clean. → `docs/methodology/layer1-listening-cups.md`, `apml/interfaces/learning-player.apml` v2.1.19
- [x] @claude-local 07-14 **Kill Stage 0 — always-visible visual LEGO-assembly for pods in main flow — SHIPPED.** Founders' decision (Tom + Aran): retired Stage-0's audio-drilled per-atom breakdown ladder from the main-flow pod path, replaced with an ALWAYS-VISIBLE visual reusing the existing LEGO-assembly engine. During a pod lap the whole dialogue TURN (glued sentences, grouped by new `computeTurnSpans`/`turnSpanForIndex` in `@ssi/core/pods`) stays on screen, each sentence lighting up as it plays; under each sentence is a LEGO-tile breakdown — one tile per `atom_map` entry, known gloss beneath, in target-sentence order ("the visuals betray the target language grammar"). New `PodTurnDisplay.vue` wraps `LegoAssembly.vue` directly (forced `cycle-type="debut"` for always-on glosses, `phase="voice2"` for no reveal-gating) rather than parallel UI. `usePodLapScheduler.ts`'s `nextLap()` no longer weaves in Stage-0 OR the agent-authored fusion-rung sub-ladder (both were the same audio-drilling shape) — every sentence enters straight at whole-sentence Stage 1. `fusionDrill.ts` itself is untouched, still load-bearing for Listening Mode's Dialogues > Drill (`useListeningPods.ts`) — a separate, learner-opt-in surface. Deleted dead runtime-weaving code once callers were gone (`buildStage0Tier`/`composeSentenceArc`/`loadStage0ClipMaps`/`stage0ViewFor`); kept `stage0Sequence.ts`'s lower-level sequencing + atom types for the admin-only Pod stage auditioner (`/admin/pod-auditioner`), unchanged. Speaking-mode pods' own DECAY/reveal model (LegoAssembly's prompt/voice1/voice2 phases) is untouched, separate concern. Did NOT touch the concurrent SEED-play-pattern files (`useLayer1Scheduler.ts`/`playerTypes.ts`/`SimplePlayer.ts`/`generateLearningScript.ts`/`toSimpleRounds.ts`). core 497 + player-vue 589 tests green (7 new `podTurns` tests), both typechecks + core build clean. → `apml/interfaces/learning-player.apml` v2.1.20, `apml/learning/listening-layers.apml`

## 🤔 Areas to think through   (open design — link the think-piece)

- [x] @web 06-14 **Flexible grouping layer — THINK-PIECE WRITTEN (`e8e688d2`):** `docs/methodology/flexible-grouping.md`. Belonging singular+hard (`school_id` FK) vs grouping plural+soft (year/dept/faculty/key-stage/house/cohort/chain, overlapping time-bounded tags — same primitive as students/teachers, one level up; lean off the rigid `groups` path-tree). Leader scope = a predicate over those tags (school-leader needs zero grouping rows; dept/year added when a school asks). Roll-up = coverage board at a higher `GROUP BY`; k-floor sovereignty carries up. **Build is the consumer-first follow-on** (open Qs in §7). → `tutor-insights.md` §5
- [ ] **Adaptation engine (M2)** — the defer / drill / consolidate budget driven by curvature. Design explored 06-13 (`docs/methodology/adaptation-budget.md`); **next code step is B4** (curvature per unit) + Layer-1 persistence, not the controller itself. → `metrics-architecture.md` §4
- [ ] **Prosody / VAD axis (M3)** — what to capture, when; gated on opt-in adoption. Orphaned VAD fields are computed-then-dropped today. → `metrics-architecture.md` §6
- [ ] **CEFR-via-calibration (M4–M5)** — the research roadmap; pilot-coupled timeframe.
- [ ] **Daily agent routines** — which sensible analyses are worth a ProMax routine. Repo-only ones (e.g. a WORKLIST groomer) work in cloud today; Supabase ones need the service key in the routine env (no Supabase MCP yet).

- [ ] **First-boot experience (PARKED think-piece, 07-02):** staged loading narrative + instant first sound + generic welcome clip + deferred tips surface → `docs/first-boot-experience.md`. Key facts already true: default course IS zho_for_eng; a 2800ms first-timer cinematic already exists to hang messages on; first-cycle audio prewarm exists (check it covers first boot). The 6s case = once-per-device INF-PLAY build on a new browser.

- [x] @web 07-14 **Group commercial model — SETTLED:** `docs/schools/group-commercial-model.md`. Unit = the teacher (not school): `y(band)` per-teacher PAYG subscription via Paddle (merchant of record, handles regional VAT/GST), no proration/true-up. Students always paid (£5, no trial). Groups are aggregating payers only, never a pricing tier — who-pays lives in Paddle's ledger, not the domain schema. No commercial caps (school creation stays uncapped). Trial: 30d single-language, clock starts at course-pick w/ ~7d browse window, 365d no-lock for minority languages, expiry → read-only never lockout. Superseded: x-per-school flat pricing, school caps as purchases, invoicing/true-up, payer objects — see doc's appendix. Pending: `y` per band, band list, annual/monthly discount, and the Paddle integration build.

- [ ] **Capacity caps at redemption (logged 07-13, NOT actionable until a policy exists):** every join in the schools tier now flows through invite codes (`invite_codes.max_uses` already live — leader invites are one-shot), so abuse/commercial control is a mint-time number, not new machinery. Two shapes when a policy lands: per-code cap (`max_uses` at mint) vs per-container cap (schools-per-group / teachers-per-school / students-per-class, checked at redemption against the live count — the version that maps to contract terms & entitlements). Single choke point = `api/code/redeem.ts`; keep it that way (no side-door join flows). Tom decides the policy numbers; do not build speculatively.

- 🤔 **College dynamics: play-as-class vs student accounts (Tom, 2026-07-14).** Schools = mostly play-as-class; colleges (16+ students with emails) may prefer creating £5 student accounts — and there may be a revenue angle that motivates them. Open questions: what's the natural college motion, does a college/teacher revenue-share (tutor-style split) make sense inside institutions, and how does that interact with the settled per-teacher PAYG model? Needs a Tom thinking session before any build.
- 🤔 **Revision ramp on class resume (NPTC promise 8d)** — promised: "a little extra revision at the start of each session." Nothing specifies trigger (time-gap? every resume?) or scope (since-last-session vs pool). Pedagogy design needed (Tom + design pass) before build.
- 🤔 **Notification/alerting primitive** — promise 10 (student-caught-up → teacher alert) needs one; ZERO notification infra exists codebase-wide. Per the BSC spine rule (no signal before its consumer), park until individual accounts see real homework use — then design the primitive once, for all future alert consumers.

## 🚧 In flight / don't collide

- teacher↔class M2M stream — **merged to dev; migration APPLIED 06-13** (edge endpoints + `20260613` live). Branch `feat/class-teachers-edge` cleaned up — don't go looking for it on the remote. Coordinate via dev before touching the class/teacher data model.
- `fix/pod-phase0-explainer-stage` — listening v2 + Phase 0 explainer; awaiting Tom's ear/merge.
- Atom-fusion **compute core** is on dev (`4cccc6f1`); the Popty persistence upstream is unbuilt (see To build).
- `worktree-agent-*` branches are parallel scratch — don't reuse those names.

## ⛔ Blocked / parked

- [x] @tom 06-13 **Migration `20260613_class_first_class_citizen.sql`** — APPLIED & verified (7 lead tags backfilled, `class_teachers` view + `is_class_teacher()` live; additive, app still reads the lead pointer). The app-read migration ownership→membership is the next step (see To build), not this.
- [x] @web 06-14 **Metrics M1 consumer — RENDER SHIPPED (`c497d54c`): `DifficultyTurnsBoard` (4th Insight board), demo-pathed, reuses the Table widget. Real path lights up when Tom applies `analytics_difficulty_turns`.** `difficultyTurns` resolver (B4 over the live series → a "needs attention" table, most-struggling-first) + admin RPC `analytics_difficulty_turns` (DRAFT — **Tom applies** to activate) + registered + tested (347 green). **Next slice:** the designed render — a `DifficultyTurnsBoard` in the Insight Engine (matches the Frostwell board bar + demo mode + `InsightsView` registration) *or* a Discovery-feed entry. Warrants the frontend-design care. → `metrics-implementation-plan.md` §1 D3 / M1
- [~] @claude-local 06-14 **Supabase cloud sentinels** — CREATED as daily ProMax routines: health pulse `trig_011pVKtRxFrVwzgLADqdaFFd` (07:00) + webhook integrity `trig_01Xvq4BJiQ11jsDozZQ6T2a2` (07:30). Clone the repo, read-only, report to the routine run history, self-report `🚧 BLOCKED` if creds absent. Gated ONLY on `SUPABASE_URL`+`SUPABASE_SERVICE_ROLE_KEY` set as ENV SECRETS on the cloud env (no secret-injection field in the trigger API, no Supabase MCP). Health-pulse smoke-test fired 06-14 → check its run history for blocked-vs-data. Slack escalation is one update away (needs a channel).

## ✅ Done (archive — groomer-managed, don't hand-edit)

- 2026-06-13 — Tutor-insights v2 (attention vs coverage; class-as-learner) + class-first-class-citizen migration drafted.
- 2026-06 — Listening mode v2 (stage modes, teleprompter, cache-horizon, glosses); Insight Engine Discovery feed at `/admin/insights`; secfix #1–16 (RLS hardening + identity bridge).

---

*Cross-SSi: this is the learning-app worklist. Popty (`ssi-dashboard-v7-clean`) and other repos get their own `WORKLIST.md` of the same shape. A daily repo-only "groomer" routine can archive `[x]`s, free stale `[~]`s, and surface shipped/dead items as a commit for review — it never silently rewrites live intent.*

## 🔨 Onboarding target-picker fixes (logged 2026-06-17) — ALL VERIFIED SHIPPED 07-02
- [x] **Welsh in the "You'll teach" target dropdown** — verified live: /api/courses/available returns cym_n/cym_s with target_lang 'cym' + gle; the heritage door (schools1) pins them first.
- [x] **"You'll teach" dropdown search bar** — shipped (`targetQuery`/`visibleTargetOptions` in Onboarding.vue).
- [x] **Auto-select single language choice** — shipped (`maybeAutoSelect` watches both `courses` and `visibleCourses`).

## 🔨 Upgrade/checkout improvements (logged 2026-06-17) — VERIFIED SHIPPED 07-02
- [x] **Paddle checkout window** — inline checkout into a sized container shipped (useSchoolCheckout `frameTarget` + UpgradeView).
- [x] **Monthly OR annual toggle** — shipped on both lanes (UpgradeView `billing-toggle`, annual price ids with graceful fallback).

## 📌 Paddle LIVE price IDs (for env-var config — not secret, client-inlined)
- Premium MONTHLY £15: `pri_01kqq85gvncyasfmfvvpcv1xfg`  → VITE_PADDLE_TEACHER_PRICE_MONTHLY
- Premium ANNUAL £150/yr: `pri_01kqq86ymc3yhm8be3w7f7kgr1`  → VITE_PADDLE_TEACHER_PRICE_ANNUAL  (set this to enable annual; school annual falls back to it)
- Student £10/mo (via tutor): `pri_01kqq89qwnsd3qwxvyybsc6ey1`  → VITE_PADDLE_STUDENT_PRICE_MONTHLY
- Student £5/mo (via school): `pri_01kv5wrc5cz17pwgeva4zk8s0r`  → VITE_PADDLE_STUDENT_SCHOOL_PRICE_MONTHLY
- School platform = same £15/£150 Premium price at quantity>1 (no separate product). Confirm annual price allows qty>1.

## 📌 Paddle STUDENT price IDs (SSi Student product) — added annual 2026-06-17
- Tutor student MONTHLY £10: `pri_01kqq89qwnsd3qwxvyybsc6ey1`  → VITE_PADDLE_STUDENT_PRICE_MONTHLY
- Tutor student ANNUAL £100/yr: `pri_01kvaj1ben739erky6zjdjsq22`  → VITE_PADDLE_STUDENT_PRICE_ANNUAL (NEW var — needs wiring + env)
- School student MONTHLY £5: `pri_01kv5wrc5cz17pwgeva4zk8s0r`  → VITE_PADDLE_STUDENT_SCHOOL_PRICE_MONTHLY
- School student ANNUAL £50/yr: `pri_01kvaj05x1y16trwvm8pdm2wcb`  → VITE_PADDLE_STUDENT_SCHOOL_PRICE_ANNUAL (NEW var — needs wiring + env)
- TODO if we want student monthly/annual choice: add a toggle in the student join flow (WithTeacher.vue) + the two new annual vars in paddle.ts, mirroring the Upgrade-page annual toggle.

## 🤔 Teacher "Learn" button — own-account learning, keep the environment (logged 2026-06-17)
- Today: TopNav "Learn" (teach mode) does router.push('/') → bare learner home, LOSES the teach nav/context.
- Want: (1) make it OBVIOUS the top-right button is the teacher's OWN account learning (not class learning) — relabel e.g. "Learn something new yourself" / "My learning", distinct affordance/icon; (2) open the player IN the teach environment so the nav stays (desktop).
- Open question (Tom flagged): MOBILE has no room for a top nav bar over the player. Proposed: desktop = player inside the teach shell (keeps TopNav, like /teach/play); mobile = full-screen player with a clear "← Back to teaching" control (AppEscape-style) instead of the full bar. Needs a route like /teach/learn (PlayerContainer, no class context = teacher's own courses).
