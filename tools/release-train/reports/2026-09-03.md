# Friday ship candidate — 2026-09-03

**40 commits on `staging` and not on `main`.** Tom decides GO or HOLD; nothing
promotes without his word (founder ruling 2026-07-30).

- candidate range: `572d716..39e9199` — [compare on GitHub](https://github.com/thomascassidyzm/ssi-learning-app/compare/main...staging)
- `dev` is 3 commit(s) ahead of `staging` (not in this candidate)
- last production commit landed 2026-09-02

## Release notes (draft)

**What's new**
- I18n(jpn,kor): localise settings.ssiFamily.
- I18n: key 357 more learner-facing strings, and gate the door behind them.
- I18n: wire 71 learner-facing literals to keys that already existed.

Draft: [`tools/release-train/notes/2026-09-03.md`](../notes/2026-09-03.md) — edit the bullets there before GO;
`promote.sh --go` stamps that file with the ship date and the promoted sha, keeping your edits.

## What ships

30 substantive commits, 10 process commits
(merges, worklist claims, promote records — no user-facing change).

### Other — 28 code + 1 docs/tests
- fix(i18n): put t in scope where the template calls it — Library sheet was blank
- i18n: key the strings staging added while dev was localising
- merge(staging→dev): back-merge staging so the promotion cannot drop its fixes
- i18n(jpn,kor): localise settings.ssiFamily — the passthrough gate's two FAILs
- i18n: the 311-key debt is paid — empty the pending-translation register
- i18n: add English-passthrough detector (second check alongside key-parity)
- …and 22 more

### Admin & internal tools — 1 code
- sentinel: never report a broken probe as "learners can't play"

## CI health

Source: the **watson-1 nightly** (03:00, `ssi-ci.timer`) — lint · player typecheck · api typecheck · player tests · api tests, out of this repo's own workflow file. GitHub Actions has been dormant since 2026-08-14 and is not consulted.

Matched per commit SHA against 21 rows covering 18 distinct commits, 2026-08-29 → 2026-09-03. **Coverage is head-of-branch nightly, not per-commit**: the nightly tests the head of dev, staging and main on the nights it runs, so a commit in the middle of a batch has no verdict of its own. A large "no verdict" count is normal and does NOT mean anything broke.

- ✅ green: 1
- ✅ red: 0
- ⬜ no verdict recorded: 39 (never the head of a branch on a night the nightly ran — untested, which is not the same as failing)

Staging head `39e9199`: **not tested at this SHA** — watson-1 nightly — no row for this commit.

## Open regressions & gates

Heuristic (not a tracker query): open `[ ]`/`[~]`/`[!]` lines in `WORKLIST.md` on
`staging`, matched on the keyword sets below. Read it as a prompt to look, not a verdict.

### Named regression / live defect (2)
- @wobble-fable 07-20 **Kill the org-switch wobble — layout stability on node home** (founder bug: 3-4 page wobbles per switch via the map rail; measure CLS per switch on deployed staging → hold-geometry fixes, never wron…
- @fable-local 07-24 **Cross-course wrong-language DETECTION sweep — RUNNING (CoS-approved, detection-only, no regeneration).** Approved under the standing "completely fix the TTS side of things for all courses" (2026-07-…

### Shipped but not stated-verified (2)
- @school-admin-view-fable 07-30 **Teacher/school_admin THE-VIEW convergence — SCHOOL_ADMIN HALF SHIPPED (founder ruling 07-30: "they should also have the hierarchical LHS WHERE YOU ARE menu").** Server: `groupTreeAuth.re…
- @claude-local 07-14 **Adaptation engine (M2)** — the defer / drill / consolidate budget driven by curvature. Design explored 06-13 (`docs/methodology/adaptation-budget.md`); B4 + Layer-1 series persistence have since SH…

### Unblocked by this promote (gated on MAIN) (2)
- **`lego_progress` UNIQUE(learner_id, lego_id) omits course_id (07-04 review).** Lego ids repeat across courses, so a multi-course learner cross-contaminates progress (and course-B inserts hit the constraint and are swal…
- **Revoke authenticated SELECT on `invite_codes` — gated on `api/admin/codes.ts` reaching MAIN.** Code strings are bearer credentials (join codes redeem into privilege grants), so any signed-in user can currently enumera…

## The decision

**GO** → run the promote in `docs/RELEASE-TRAIN.md` (`tools/release-train/promote.sh`).
The deploy sentinel picks up the main push within 3 minutes and watches production for 2h.

**HOLD** → say what to fix; the candidate rolls to next Friday and grows.

