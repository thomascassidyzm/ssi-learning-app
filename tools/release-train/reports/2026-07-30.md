# Friday ship candidate — 2026-07-30

**150 commits on `staging` and not on `main`.** Tom decides GO or HOLD; nothing
promotes without his word (founder ruling 2026-07-30).

- candidate range: `b36caf6..ce7d2f7` — [compare on GitHub](https://github.com/thomascassidyzm/ssi-learning-app/compare/main...staging)
- `dev` is 0 commit(s) ahead of `staging` (not in this candidate)
- last production commit landed 2026-07-27

## Release notes (draft)

**What's new**
- School leaders and admins land on the unified dashboard view, with the same navigation everywhere.
- Switching course is near-instant — no more waiting through a loading sequence.
- Guided walkthroughs of the product, for learners, teachers and leaders.

**Fixes**
- Guided walkthroughs no longer collide with playback.
- The play button no longer falls out of step with what is actually playing.
- Loading and resting messages read like sentences rather than labels.
- Opening text is no longer cut off mid-word.

Draft: [`tools/release-train/notes/2026-07-30.md`](../notes/2026-07-30.md) — edit the bullets there before GO;
`promote.sh --go` stamps that file with the ship date and the promoted sha, keeping your edits.

## What ships

86 substantive commits, 64 process commits
(merges, worklist claims, promote records — no user-facing change).

### Player & playback — 14 code + 2 docs/tests
- course-switch READY in 2-3s: kill the cinematic floor on switches + ready-gate the full-course walk
- player: never cut the awakening typewriter mid-word
- refactor(player): belt position derives from the engine (M9) — INF-PLAY freeze kept as an explicit intent
- refactor(player): round/cycle position derives from the engine (M3) — pre-engine resume intent consumed once at init
- refactor(player): reactive queue is PULLED from the engine (M4) — three hand-mirrored algorithms deleted
- refactor(player): session timer gates on the named audible signal (M8) — hand-ORed duplicate list deleted
- …and 8 more

### Other — 10 code + 6 docs/tests
- THE VIEW: WHERE-YOU-ARE rail stability across Overview <-> Insights
- release-train: fix the notes test command for CI node 20, and gate auto-merge on it too
- release-train: notes shape — up to 3 features by user significance, then the fixes
- release-train: automatic release notes — drafted Thursday, stamped final on promote
- release-train: Friday ship candidate report 2026-07-30
- release-train: rev/path separators on git log — the stray origin/ dir in the repo root shadows the remote name
- …and 4 more

### Family plan & billing — 9 code + 4 docs/tests
- family: teach the offline-lease test mock .is() — the family resolver's membership query shape
- family: retroactive family_members migration record (ALREADY APPLIED 2026-07-10, do not re-apply)
- family: re-anchor the et-token minting note to the try-link mint reality
- family: regenerate lockfile for qrcode deps (dev-side regen, not branch lockfile reapply)
- family: forward-pointing note for entitlement-token minting (spec §3)
- family: UI — Settings family page, paywall option, covered-by-family branch (FAMILY-PLAN-SPEC.md §4)
- …and 3 more

### Schools & teachers — 4 code + 9 docs/tests
- schools nav unification: school_admin lands on THE VIEW — third persona through the door
- teacher home classes-first: PAC primary, stats demoted (founder ruling 07-30)
- schools teacher surface: nav persists in the player, slim playing-as chip, guided-look wording, exits land home
- schools nav unification: govt_admin tabs land on THE VIEW, flat views retired behind redirects

### Walkthrough & onboarding — 3 code + 17 docs/tests
- explainer: How-this-works becomes the single surfacing point, with a subtle discoverability throb
- fix(walkthrough): viewport-safe overlay on oversized anchors; ledger anchor moves to its head
- feat(walkthrough): compiled walkthrough engine + first 5 walks

### Insights & metrics — 2 code + 2 docs/tests
- feat(vad): cycle_prosody carries intermediate features — compact energy contour + full timing (founder schema steer)
- feat(vad): cycle_prosody event + SimplePlayer VAD timing wiring (phase 1)

### Admin & internal tools — 2 code + 1 docs/tests
- deploy sentinel stage 1: post-main-push fallout watcher (cron on watson-1)
- feat(admin): inline guards on the ssi_admin danger verbs — the scout §4 †-worklist closed

Docs/tests only, no code: Infra, cache & offline (1).

## CI health

Verify (lint · player typecheck · api typecheck · player tests · api tests), matched per commit SHA across 160 recent runs:

- ✅ green: 93
- ❌ red: 2
- ⬜ no run recorded: 55 (mostly merge commits, which CI does not run on individually)

**Red commits:**
- `be5a7db` failure — release-train: notes shape — up to 3 features by user significance, then the fixes
- `27f674a` failure — release-train: automatic release notes — drafted Thursday, stamped final on promote

Staging head `ce7d2f7` combined status: **success** — Vercel: success.

## Open regressions & gates

Heuristic (not a tracker query): open `[ ]`/`[~]`/`[!]` lines in `WORKLIST.md` on
`staging`, matched on the keyword sets below. Read it as a prompt to look, not a verdict.

### Named regression / live defect (2)
- @wobble-fable 07-20 **Kill the org-switch wobble — layout stability on node home** (founder bug: 3-4 page wobbles per switch via the map rail; measure CLS per switch on deployed staging → hold-geometry fixes, never wron…
- @fable-local 07-24 **Cross-course wrong-language DETECTION sweep — RUNNING (CoS-approved, detection-only, no regeneration).** Approved under the standing "completely fix the TTS side of things for all courses" (2026-07-…

### Shipped but not stated-verified (2)
- @school-admin-view-fable 07-30 **Teacher/school_admin THE-VIEW convergence — SCHOOL_ADMIN HALF SHIPPED (founder ruling 07-30: "they should also have the hierarchical LHS WHERE YOU ARE menu").** Server: `groupTreeAuth.re…
- @claude-local 07-14 **Adaptation engine (M2)** — the defer / drill / consolidate budget driven by curvature. Design explored 06-13 (`docs/methodology/adaptation-budget.md`); B4 + Layer-1 series persistence have since SH…

### Unblocked by this promote (gated on MAIN) (2)
- **`lego_progress` UNIQUE(learner_id, lego_id) omits course_id (07-04 review).** Lego ids repeat across courses, so a multi-course learner cross-contaminates progress (and course-B inserts hit the constraint and are swal…
- **Revoke authenticated SELECT on `invite_codes` — gated on `api/admin/codes.ts` reaching MAIN.** Code strings are bearer credentials (join codes redeem into privilege grants), so any signed-in user can currently enumera…

## The decision

**GO** → run the promote in `docs/RELEASE-TRAIN.md` (`tools/release-train/promote.sh`).
The deploy sentinel picks up the main push within 3 minutes and watches production for 2h.

**HOLD** → say what to fix; the candidate rolls to next Friday and grows.

