# Friday ship candidate — 2026-09-10

**144 commits on `staging` and not on `main`.** Tom decides GO or HOLD; nothing
promotes without his word (founder ruling 2026-07-30).

- candidate range: `0ac2715..bab9011` — [compare on GitHub](https://github.com/thomascassidyzm/ssi-learning-app/compare/main...staging)
- `dev` is 4 commit(s) ahead of `staging` (not in this candidate)
- last production commit landed 2026-09-10

## Release notes (draft)

**What's new**
- Staff run on the school's own clock, 365 or 30 (job #792).
- School staff get the school's own cover, course for course (job #792).
- Teacher class-coverage + kind-aware trial restore (job #788).

**Fixes**
- Offline downloads are more reliable.
- A teacher can no longer delete their school.
- Settings stops describing a pill that no longer exists.
- Name the Welsh dialect in an org's granted-courses list.
- The free year covers the courses it bought, and says so.
- A funded free year is never shown a price.
- A few questions, and a picker of only the granted courses.
- Say the Welsh course is free, and land the learner in it.
- Kai's four onboarding copy fixes.
- The enrolment link carries the wide per-IP budget, so the eleventh learner in the room gets in.
- The class roster's practice time reads the playback ledger.
- Admin and schools practice minutes read the playback ledger.

Draft: [`tools/release-train/notes/2026-09-10.md`](../notes/2026-09-10.md) — edit the bullets there before GO;
`promote.sh --go` stamps that file with the ship date and the promoted sha, keeping your edits.

## What ships

130 substantive commits, 14 process commits
(merges, worklist claims, promote records — no user-facing change).

### Other — 62 code + 12 docs/tests
- CI: the three reds on dev, and the toolchain break that hid them
- android shell: the default origin is staging, production is asked for out loud
- merge: view-as narrowed to a viewer — read-only, one door, page list (job #793)
- view-as pages: never offer a link that leads somewhere false
- merge dev
- view-as: a viewer with a hard edge, not an identity switch
- …and 56 more

### Schools & teachers — 24 code + 10 docs/tests
- merge: staff run on the school's own clock, 365 or 30 (job #792)
- merge: staff run on the school's own clock, 365 or 30 (job #792)
- schools: staff run on the school's clock, 365 days or 30
- merge: school staff get the school's own cover, course for course (job #792)
- merge: school staff get the school's own cover, course for course (job #792)
- schools: staff get their school's own cover, course for course
- …and 18 more

### Admin & internal tools — 7 code + 1 docs/tests
- admin: the admin gate stands back while view-as is switching on
- admin: give the founder back view-as — see every page as any role
- admin surface: give the Handbook a door, and reflow Ways in for a phone
- admin surface: give the Handbook a door, and reflow Ways in for a phone
- fix(admin): name the Welsh dialect in an org's granted-courses list
- fix(admin): show the courses an org grants through its enrolment policy
- …and 1 more

### Player & playback — 5 code
- merge: admin effective-access reads the player's own resolver (job #788)
- merge: admin effective-access reads the player's own resolver (job #788)
- admin: the user page reads the same access the player does
- merge: the learner's own plan hours read the playback ledger, not wall clock (job #484) into dev
- fix(minutes): the learner's own plan hours read the playback ledger

### Infra, cache & offline — 4 code
- merge: offline download asks the same question as play (job #794)
- merge: offline download asks the same question as play (job #794)
- offline: download asks the same question as play, and never outlives the cover
- i18n: the nightly goes green — 34 new keys enrol, the Google door speaks, and the SW test stops pinning a retired rule

### Family plan & billing — 3 code + 2 docs/tests
- Merge the funded-year price suppression, reconciled with the scoped picker
- fix(billing): the free year covers the courses it bought, and says so
- fix(billing): a funded free year is never shown a price

## CI health

Source: the **watson-1 nightly** (03:00, `ssi-ci.timer`) — lint · player typecheck · api typecheck · player tests · api tests, out of this repo's own workflow file. GitHub Actions has been dormant since 2026-08-14 and is not consulted.

Matched per commit SHA against 25 rows covering 21 distinct commits, 2026-08-29 → 2026-09-10. **Coverage is head-of-branch nightly, not per-commit**: the nightly tests the head of dev, staging and main on the nights it runs, so a commit in the middle of a batch has no verdict of its own. A large "no verdict" count is normal and does NOT mean anything broke.

- ✅ green: 0
- ❌ red: 3
- ⬜ no verdict recorded: 141 (never the head of a branch on a night the nightly ran — untested, which is not the same as failing)

**Red commits:**
- `4826414` red — merge: view-as narrowed to a viewer — read-only, one door, page list (job #793)
- `ee1c947` red — merge origin/dev
- `1c979da` red — promote: dev → staging — scoped picker: no premium header, Welsh-not-Chinese fallback (job #649)

Staging head `bab9011`: **not tested at this SHA** — watson-1 nightly — no row for this commit.

## Open regressions & gates

Heuristic (not a tracker query): open `[ ]`/`[~]`/`[!]` lines in `WORKLIST.md` on
`staging`, matched on the keyword sets below. Read it as a prompt to look, not a verdict.

### Named regression / live defect (2)
- @wobble-fable 07-20 **Kill the org-switch wobble — layout stability on node home** (founder bug: 3-4 page wobbles per switch via the map rail; measure CLS per switch on deployed staging → hold-geometry fixes, never wron…
- @fable-local 07-24 **Cross-course wrong-language DETECTION sweep — RUNNING (CoS-approved, detection-only, no regeneration).** Approved under the standing "completely fix the TTS side of things for all courses" (2026-07-…

### Shipped but not stated-verified (2)
- @school-admin-view-fable 07-30 **Teacher/school_admin THE-VIEW convergence — SCHOOL_ADMIN HALF SHIPPED (founder ruling 07-30: "they should also have the hierarchical LHS WHERE YOU ARE menu").** Server: `groupTreeAuth.re…
- @claude-local 07-14 **Adaptation engine (M2)** — the defer / drill / consolidate budget driven by curvature. Design explored 06-13 (`docs/methodology/adaptation-budget.md`); B4 + Layer-1 series persistence have since SH…

### Unblocked by this promote (gated on MAIN) (2)
- **`lego_progress` UNIQUE(learner_id, lego_id) omits course_id (07-04 review).** Lego ids repeat across courses, so a multi-course learner cross-contaminates progress (and course-B inserts hit the constraint and are swal…
- **Revoke authenticated SELECT on `invite_codes` — gated on `api/admin/codes.ts` reaching MAIN.** Code strings are bearer credentials (join codes redeem into privilege grants), so any signed-in user can currently enumera…

## The decision

**GO** → run the promote in `docs/RELEASE-TRAIN.md` (`tools/release-train/promote.sh`).
The deploy sentinel picks up the main push within 3 minutes and watches production for 2h.

**HOLD** → say what to fix; the candidate rolls to next Friday and grows.

