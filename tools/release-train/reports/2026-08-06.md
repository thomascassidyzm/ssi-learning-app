# Friday ship candidate — 2026-08-06

**70 commits on `staging` and not on `main`.** Tom decides GO or HOLD; nothing
promotes without his word (founder ruling 2026-07-30).

- candidate range: `90aa56f..003cc2e` — [compare on GitHub](https://github.com/thomascassidyzm/ssi-learning-app/compare/main...staging)
- `dev` is 31 commit(s) ahead of `staging` (not in this candidate)
- last production commit landed 2026-08-06

## Release notes (draft)

**What's new**
- Invitations work as links, with a short code kept as the fallback.
- A listening exercise goes straight in.
- Listening mode lives under Settings, not the mode popup.

**Fixes**
- The player plays what it has.
- The invite email comes from no-one, and says one thing.
- An unnamed leader never signs the invite with their email.
- A deep link never follows a course switch.
- Back-merge: main -> staging after the offline fix-lane push.

Draft: [`tools/release-train/notes/2026-08-06.md`](../notes/2026-08-06.md) — edit the bullets there before GO;
`promote.sh --go` stamps that file with the ship date and the promoted sha, keeping your edits.

## What ships

49 substantive commits, 21 process commits
(merges, worklist claims, promote records — no user-facing change).

### Other — 15 code + 5 docs/tests
- demo-data: scope the suite's reset to its own scenarios, stop eating sibling demo worlds
- fix(orgs): rename warning says Renaming, not Creating
- feat(orgs): the duplicate-name warning on rename too
- fix(orgs): Enter must not silently confirm a duplicate org name
- fix(orgs): group deletion walks parent_id, never the duplicate-prone slug path
- fix(orgs): Enter must not silently confirm a duplicate org name
- …and 9 more

### Player & playback — 8 code + 5 docs/tests
- fix(player): the player plays what it has — no single item can stall a session
- feat(player): a listening exercise goes straight in — no intro popup
- fix(audio): the script walk stamps revision refs too, so the cache fast path stops serving stale clips
- feat(player): listening mode lives under Settings, not the mode popup
- fix(player): a cycle deep link lands on the row that was clicked
- feat(player): a deep-linked launch runs on learner defaults
- …and 2 more

### Schools & teachers — 8 code + 2 docs/tests
- demo VAD: keep the empty-state UI probe, note the school-dashboard gap
- feat(demo-data): VAD data for ~50% of school learners, absent for the rest
- fix(invites): the invite email comes from no-one, and says one thing
- merge: the branded org invite email, back-merged to staging
- fix(invites): an unnamed leader never signs the invite with their email
- feat(invites): the org invite email is ours — branded, via Resend
- …and 2 more

### Insights & metrics — 2 code + 2 docs/tests
- demo-data: additive VAD top-up over the IME world's existing 381 learners
- sentinel: judge telemetry against the quietest-but-one prior week, not the median

### Admin & internal tools — 1 code
- sentinel: gate volume-crater alerts behind a real browser play-through

### Infra, cache & offline — 1 code
- back-merge: main -> staging after the offline fix-lane push

## CI health

Verify (lint · player typecheck · api typecheck · player tests · api tests), matched per commit SHA across 362 recent runs:

- ✅ green: 49
- ✅ red: 0
- ⬜ no run recorded: 21 (mostly merge commits, which CI does not run on individually)

Staging head `003cc2e` combined status: **success** — Vercel: success.

## Open regressions & gates

Heuristic (not a tracker query): open `[ ]`/`[~]`/`[!]` lines in `WORKLIST.md` on
`staging`, matched on the keyword sets below. Read it as a prompt to look, not a verdict.

### Named regression / live defect (2)
- @wobble-fable 07-20 **Kill the org-switch wobble — layout stability on node home** (founder bug: 3-4 page wobbles per switch via the map rail; measure CLS per switch on deployed staging → hold-geometry fixes, never wron…
- @fable-local 07-24 **Cross-course wrong-language DETECTION sweep — RUNNING (CoS-approved, detection-only, no regeneration).** Approved under the standing "completely fix the TTS side of things for all courses" (2026-07-…

### Shipped but not stated-verified (2)
- @school-admin-view-fable 07-30 **Teacher/school_admin THE-VIEW convergence — SCHOOL_ADMIN HALF SHIPPED (founder ruling 07-30: "they should also have the hierarchical LHS WHERE YOU ARE menu").** Server: `groupTreeAuth.re…
- @claude-local 07-14 **Adaptation engine (M2)** — the defer / drill / consolidate budget driven by curvature. Design explored 06-13 (`docs/methodology/adaptation-budget.md`); B4 + Layer-1 series persistence have since SH…

### Unblocked by this promote (gated on MAIN) (2)
- **`lego_progress` UNIQUE(learner_id, lego_id) omits course_id (07-04 review).** Lego ids repeat across courses, so a multi-course learner cross-contaminates progress (and course-B inserts hit the constraint and are swal…
- **Revoke authenticated SELECT on `invite_codes` — gated on `api/admin/codes.ts` reaching MAIN.** Code strings are bearer credentials (join codes redeem into privilege grants), so any signed-in user can currently enumera…

## The decision

**GO** → run the promote in `docs/RELEASE-TRAIN.md` (`tools/release-train/promote.sh`).
The deploy sentinel picks up the main push within 3 minutes and watches production for 2h.

**HOLD** → say what to fix; the candidate rolls to next Friday and grows.

