# Release sheet automation: feasibility first

The six sheet IDs and the existing recorder/gate remain the contract. No
promotion, gate exemption, or Android verdict inferred from Chromium.

1. Read the sheet, fixture seeder, journeys README/library, recorder, gate,
   Android origin probe and the machine's nightly CI/reporting implementation.
2. Prove the blank fresh fixture reaches a visible white-to-yellow transition
   with natural playback, media-clock advancement, a screenshot and stable
   deployed/running build identity. A bounded incomplete probe is not a failure
   of the application and is never a pass. Review this evidence before expansion.
3. Exercise the wall fixture and dismissal; premium beyond the wall with both
   phrase and commentary pause/resume, requiring new media advancement each time.
4. Capture Settings top/bottom and actual address text; observe five real minutes
   of backgrounding; interrupt and restore network mid-round. Desktop visibility
   or CDP lifecycle simulation must not be labelled Android OS backgrounding.
5. Publish twelve-cell coverage. All six Android cells remain not checked until
   a real app driver can execute them. A Chromium origin seam probe is separate
   supporting evidence, never a second real platform or a clean comparison.
6. Prefer a sibling runner: the build CI and this behavioural pass have different
   execution costs and evidence. Match its state/history schema and serial,
   niced, directory-lock convention. Proposed nightly slot: 00:45 UTC. Mark
   machine records in tester/recorded_by; never overwrite an existing human pass.
7. Carry the existing gate via reviewed ordinary dev-to-staging integration.
   The next staging-to-main promotion then requires a committed complete pass.

Current constraints verified 10 September 2026: no /dev/kvm; cron read denied;
no conversation token supplied for progress/publishing. The password file is
readable. The scratch root is read-only here, but its tmp child is writable.
Full Chrome fails on this job's long Unix socket path; the already installed
headless shell starts using the journeys library's shared-library environment.
No package download, full suite or production deployment is needed.
