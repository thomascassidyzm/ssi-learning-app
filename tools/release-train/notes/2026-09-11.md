<!-- release-notes:header -->
# Release notes — DRAFT for the next ship

Drafted 2026-09-11 alongside [the candidate report](./../reports/2026-09-11.md) — 209 commits, candidate head `fb17bea`. **Edit these bullets freely**; Friday's promote stamps the header and leaves the text alone.

Up to three headline features — player/learner-facing surfaces weighted first — then one
catch-all section for everything else that shipped. Deliberately under-claiming: a commit
that cannot be translated confidently into user-facing language is dropped rather than
guessed at. Generated by `tools/release-train/release-notes.mjs`.
<!-- /release-notes:header -->

## What's new

- Invitations work as links, with a short code kept as the fallback.
- Staff run on the school's own clock, 365 or 30 (job #792).
- School staff get the school's own cover, course for course (job #792).

## Other stuff and bug fixes

- Merge cs/90: close client-side class-insert bypass of course entitlement.
- Teacher class-coverage + kind-aware trial restore (job #788).
- The learner's own plan hours read the playback ledger, not wall clock (job #484) into dev.
- Merge cs/177: school practice time (rebased, conflict resolved) -- real in-app session time.
- Node home and teacher dashboard lead with MINUTES IN THE APP; audio-played demoted, labelled plainly.
- Class practice time is IN-APP SESSION TIME off the diary, audio-played demoted to secondary.
- Merge cs/159: Monday-morning school board -- phrases spoken, classes practising.
- Class practice is phrases spoken off the diary, never hours off class-account sessions.
- Three more questions, each killing its fossil: who is leaving, which courses matter, is the app working.
- Merge cs/79: school door premium chip copy.
- School door: every language row states what you get.
- Vocab glossary: close the layer-1 voice order.
- Vocabulary pointers: fold in the layer-1 and listening-mode entry points.
- Minting a learner: real and included, gifted rather than excluded.
- Question 4: which bits of a course make people stumble, and the k-floor that keeps it honest.
- The user page reads the same access the player does.
- A teacher gets the course she teaches, and restore gives the right window.
- The app tells a learner who they are.
- Merge the funded-year price suppression, reconciled with the scoped picker.
- A sign-up link can name the dialect, and skip the question.
- The remembered course is written down with its origin.
- Origin/dev into the Canolfan leader-row/UI branch.
- A funded org can have a leader, assigned to an account that already exists.
- Kai's confirmation of the two Canolfan Welsh course codes, and an end-to-end proof of the enrolment pipeline (job #518) into dev.
- Offline downloads are more reliable.
- Merge cs/71-fix-drill-mode-label-copy: correct Drill mode label copy.
- A teacher can no longer delete their school.
- Settings stops describing a pill that no longer exists.
- The browser can no longer insert a class, so the course gate cannot be walked round.
- A class can no longer be opened on a premium course the node has not paid for.
- Name the Welsh dialect in an org's granted-courses list.
- The free year covers the courses it bought, and says so.
- A funded free year is never shown a price.
- A few questions, and a picker of only the granted courses.
- Say the Welsh course is free, and land the learner in it.
- Kai's four onboarding copy fixes.
- The enrolment link carries the wide per-IP budget, so the eleventh learner in the room gets in.
- The class roster's practice time reads the playback ledger.
- Admin and schools practice minutes read the playback ledger.

<!-- release-notes:draft-only -->
## Draft-only: coverage

3 feature headline(s) + 39 other-stuff line(s) from 189 substantive commits (20 process commits never considered).
This section is stripped when the notes are finalised.

**Dropped — names nothing a user can see (79):**
- Merge cs/172: org intelligence surface -- practising, quiet, journey
- feat(org-intel): give orgs the intelligence surface, scoped to themselves — practising, quiet, journey
- Merge cs/168: audible check measures sound, not time
- fix(e2e): the audible check measures sound, not time
- feat(school-board): the Monday-morning board — phrases spoken, classes practising, what they practised
- Question 6, one person's story, with the verbs — and the verb that corrects a flag
- Intel anchors described: every data-intel element carries its HANDBOOK sentence, and the pack knows which surface it be…
- Merge cs/129: walk freshness stamp over both anchor namespaces
- …and 71 more

**Dropped — internal kind (docs/tests/chore/refactor) (32):**
- test(security): SEC0901-A-04b admits create-class's converged callerUserId, structurally, and censuses callers
- test(node-home): CLASS-PRACTICE pin asserts the in-app-time headline, with ordering pinned harder
- test(router): LEARNER-PAGE-DEAD pin follows the redirect chain to the person question
- docs(e2e): record the audible-energy fix, its thresholds and its impact count
- test(subscription): teach main's platform-admin mock about dev's free access
- CI: the three reds on dev, and the toolchain break that hid them
- CI: the three reds on dev, and the toolchain break that hid them
- test(schools): teach the scope-resolver mock the list-shaped school-tag query
- …and 24 more

**Dropped — plumbing commit (tests, mocks, notes, lockfiles) (9):**
- The grammar as tests: fossils that redirect by rule, one metric one page, no hex, two taps
- walkthrough: tests for the walk-step stamp and both anchor namespaces
- back-merge: main -> staging, so the Friday promotion reaches the human-test gate
- test accounts: the three Colombo fixtures, seeded so they persist
- One population resolver for the intelligence surface, and its one test
- i18n: the nightly goes green — 34 new keys enrol, the Google door speaks, and the SW test stops pinning a retired rule
- merge: funder-export leader-access test coverage (job #607) into dev
- fix(test): index the last button rather than Array.at, which is outside this package's TS lib
- …and 1 more

**Dropped — internal machinery in the claim (6):**
- fix(explainer): re-author the three class-practice rules on the in-app-time payload, recompile the pack
- Merge cs/154: fix class-practice dead table read
- fix(the-view): class practice is read off the live class-entity spine, not the dead class_sessions table
- merge: admin effective-access reads the player's own resolver (job #788)
- merge: admin effective-access reads the player's own resolver (job #788)
- fix(admin): show the courses an org grants through its enrolment policy

**Dropped — nothing left after stripping jargon (6):**
- merge dev
- merge dev
- merge dev
- merge dev
- feat(schools): named seats — the admin types a name and hands over a code
- feat(schools): named seats — the admin types a name and hands over a code

**Dropped — shipped but not switched on for users (3):**
- One shell for the internal surfaces: the dark admin bar dies, the questions ride the white bar, the rail IS the org map…
- fix(auth): the dark Google door says so, instead of a raw Supabase 400
- feat(auth): a Google door above the email field, built and dark

<!-- /release-notes:draft-only -->
