<!-- release-notes:header -->
# Release notes — DRAFT for the next ship

Drafted 2026-08-06 alongside [the candidate report](./../reports/2026-08-06.md) — 70 commits, candidate head `003cc2e`. **Edit these bullets freely**; Friday's promote stamps the header and leaves the text alone.

Up to three headline features — player/learner-facing surfaces weighted first — then one
catch-all section for everything else that shipped. Deliberately under-claiming: a commit
that cannot be translated confidently into user-facing language is dropped rather than
guessed at. Generated by `tools/release-train/release-notes.mjs`.
<!-- /release-notes:header -->

## What's new

- Invitations work as links, with a short code kept as the fallback.
- A listening exercise goes straight in.
- Listening mode lives under Settings, not the mode popup.

## Other stuff and bug fixes

- VAD data for ~50% of school learners, absent for the rest.
- The branded org invite email, back-merged to staging.
- The org invite email is ours.
- A deep-linked launch runs on learner defaults.
- Accept a production deep link to a round and cycle.
- Promote dev → staging: /znotes live-copy pass.
- Orgs door: left pane sings in the www voice.
- Orgs door: adult-learner proof replaces the schools pupils stat.
- The player plays what it has.
- The invite email comes from no-one, and says one thing.
- An unnamed leader never signs the invite with their email.
- A deep link never follows a course switch.
- Back-merge: main -> staging after the offline fix-lane push.

<!-- release-notes:draft-only -->
## Draft-only: coverage

3 feature headline(s) + 13 other-stuff line(s) from 49 substantive commits (21 process commits never considered).
This section is stripped when the notes are finalised.

**Dropped — internal kind (docs/tests/chore/refactor) (16):**
- test(e2e): the straight-in probe taps the real transport button
- docs(player): diagnosis of the 'with you' hard stop and the bounds that close it
- docs(audio): the versioned-ref audit + the APML section the cutover never got
- docs(apml): the listening overlay's entry point is Settings, not the tray
- test(e2e): listening is entered from Settings, and a probe that proves it
- docs: demo VAD run report — add the live UI verification of both halves
- docs: demo VAD regeneration run report (2026-08-06)
- docs(apml): extend the duplicate-name spec to rename
- …and 8 more

**Dropped — names nothing a user can see (16):**
- fix(audio): the script walk stamps revision refs too, so the cache fast path stops serving stale clips
- demo-data: additive VAD top-up over the IME world's existing 381 learners
- sentinel: gate volume-crater alerts behind a real browser play-through
- sentinel: judge telemetry against the quietest-but-one prior week, not the median
- demo-data: scope the suite's reset to its own scenarios, stop eating sibling demo worlds
- fix(orgs): rename warning says Renaming, not Creating
- feat(orgs): the duplicate-name warning on rename too
- fix(orgs): Enter must not silently confirm a duplicate org name
- …and 8 more

**Dropped — plumbing commit (tests, mocks, notes, lockfiles) (1):**
- demo VAD: keep the empty-state UI probe, note the school-dashboard gap

<!-- /release-notes:draft-only -->
