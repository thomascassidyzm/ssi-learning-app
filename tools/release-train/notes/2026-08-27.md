<!-- release-notes:header -->
# Release notes — DRAFT for the next ship

Drafted 2026-08-27 alongside [the candidate report](./../reports/2026-08-27.md) — 34 commits, candidate head `9fae8c1`. **Edit these bullets freely**; Friday's promote stamps the header and leaves the text alone.

Up to three headline features — player/learner-facing surfaces weighted first — then one
catch-all section for everything else that shipped. Deliberately under-claiming: a commit
that cannot be translated confidently into user-facing language is dropped rather than
guessed at. Generated by `tools/release-train/release-notes.mjs`.
<!-- /release-notes:header -->

## What's new

- Self-hosted fonts (A-265) + automatic variant-course flags.
- Variant courses resolve their own flag automatically.
- Every national variant course flies its own flag.

## Other stuff and bug fixes

- Handler map for 2026-08-25 + SEC25-X-01/02 on the ungated round-map.
- A variant course keeps its own name inside the session.

<!-- release-notes:draft-only -->
## Draft-only: coverage

3 feature headline(s) + 2 other-stuff line(s) from 32 substantive commits (2 process commits never considered).
This section is stripped when the notes are finalised.

**Dropped — names nothing a user can see (17):**
- feat(fonts): self-host the web fonts, precache the UI's own faces
- fix(i18n): a saved interface language must be APPLIED on boot, not just remembered
- feat(flags): variant rows in the catalogue fly their flag too
- fix(build): dev tip could not resolve utils/courseDisplayName
- feat(flags): a language variant can fly its own flag
- fix(boot): no font may hold the app hostage on a weak signal
- audit(security): reconcile the verdict counts and the D-02 cost column
- audit(security): correct SEC25-D-02 — the _by_course variant needs no learner UUID
- …and 9 more

**Dropped — nothing left after stripping jargon (4):**
- audit(security): synthesis — ranked findings, gaps, and the one decision
- audit(security): SEC25-X-03 — escalate SEC25-A-01, the throttle guards the ssi_admin door
- security(audit): Area A — the API delta since the 08-22 audit
- audit(security): SEC25-X-02 — evidence that the silent key swap is undetectable

**Dropped — plumbing commit (tests, mocks, notes, lockfiles) (3):**
- audit(security): update the pinned security-test roster for the merged audit areas
- audit(security): merge Area D — DB posture, repo hygiene, test-machinery integrity (#470)
- security(audit-0825-d): DB posture + repo hygiene + test-machinery integrity (Area D)

**Dropped — internal kind (docs/tests/chore/refactor) (2):**
- test(boot): hang the THIRD-PARTY requests too — the blind spot that let this through twice
- docs(security): Area C reconciliation — 2026-08-11 audit findings vs dev, 2026-08-25

**Dropped — shipped but not switched on for users (1):**
- audit(security): README scaffold — why a fourth audit, and the partition

<!-- /release-notes:draft-only -->
