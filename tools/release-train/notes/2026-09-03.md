<!-- release-notes:header -->
# Release notes — DRAFT for the next ship

Drafted 2026-09-03 alongside [the candidate report](./../reports/2026-09-03.md) — 40 commits, candidate head `39e9199`. **Edit these bullets freely**; Friday's promote stamps the header and leaves the text alone.

Up to three headline features — player/learner-facing surfaces weighted first — then one
catch-all section for everything else that shipped. Deliberately under-claiming: a commit
that cannot be translated confidently into user-facing language is dropped rather than
guessed at. Generated by `tools/release-train/release-notes.mjs`.
<!-- /release-notes:header -->

## What's new

- I18n(jpn,kor): localise settings.ssiFamily.
- I18n: key 357 more learner-facing strings, and gate the door behind them.
- I18n: wire 71 learner-facing literals to keys that already existed.

## Other stuff and bug fixes

- I18n(tel): a Telugu interface, closing the last of the three uncovered courses.

<!-- release-notes:draft-only -->
## Draft-only: coverage

3 feature headline(s) + 1 other-stuff line(s) from 30 substantive commits (10 process commits never considered).
This section is stripped when the notes are finalised.

**Dropped — names nothing a user can see (24):**
- fix(i18n): put t in scope where the template calls it — Library sheet was blank
- i18n: key the strings staging added while dev was localising
- merge(staging→dev): back-merge staging so the promotion cannot drop its fixes
- i18n: the 311-key debt is paid — empty the pending-translation register
- i18n: add English-passthrough detector (second check alongside key-parity)
- Merge i18n:fill — the tool that satisfies the locale-parity gate
- i18n(fill): resolve the Claude CLI off PATH, and say where auth comes from
- i18n: pnpm i18n:fill — the tool that satisfies the locale-parity gate
- …and 16 more

**Dropped — plumbing commit (tests, mocks, notes, lockfiles) (1):**
- sentinel: never report a broken probe as "learners can't play"

**Dropped — internal kind (docs/tests/chore/refactor) (1):**
- docs(i18n): the render check behind the Kannada/Telugu font decision

<!-- /release-notes:draft-only -->
