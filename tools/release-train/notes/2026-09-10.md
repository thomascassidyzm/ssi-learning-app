<!-- release-notes:header -->
# Release notes — DRAFT for the next ship

Drafted 2026-09-10 alongside [the candidate report](./../reports/2026-09-10.md) — 144 commits, candidate head `bab9011`. **Edit these bullets freely**; Friday's promote stamps the header and leaves the text alone.

Up to three headline features — player/learner-facing surfaces weighted first — then one
catch-all section for everything else that shipped. Deliberately under-claiming: a commit
that cannot be translated confidently into user-facing language is dropped rather than
guessed at. Generated by `tools/release-train/release-notes.mjs`.
<!-- /release-notes:header -->

## What's new

- Staff run on the school's own clock, 365 or 30 (job #792).
- School staff get the school's own cover, course for course (job #792).
- Teacher class-coverage + kind-aware trial restore (job #788).

## Other stuff and bug fixes

- The learner's own plan hours read the playback ledger, not wall clock (job #484) into dev.
- The user page reads the same access the player does.
- A teacher gets the course she teaches, and restore gives the right window.
- The app tells a learner who they are.
- Merge the funded-year price suppression, reconciled with the scoped picker.
- A sign-up link can name the dialect, and skip the question.
- The remembered course is written down with its origin.
- Origin/dev into the Canolfan leader-row/UI branch.
- A funded org can have a leader, assigned to an account that already exists.
- Kai's confirmation of the two Canolfan Welsh course codes, and an end-to-end proof of the enrolment pipeline (job #518) into dev.
- Offline downloads are more reliable.
- A teacher can no longer delete their school.
- Settings stops describing a pill that no longer exists.
- Name the Welsh dialect in an org's granted-courses list.
- The free year covers the courses it bought, and says so.
- A funded free year is never shown a price.
- A few questions, and a picker of only the granted courses.
- Say the Welsh course is free, and land the learner in it.
- Kai's four onboarding copy fixes.
- The enrolment link carries the wide per-IP budget, so the eleventh learner in the room gets in.
- The class roster's practice time reads the playback ledger.
- Admin and schools practice minutes read the playback ledger.

<!-- release-notes:draft-only -->
## Draft-only: coverage

3 feature headline(s) + 22 other-stuff line(s) from 130 substantive commits (14 process commits never considered).
This section is stripped when the notes are finalised.

**Dropped — names nothing a user can see (54):**
- android shell: the default origin is staging, production is asked for out loud
- merge: view-as narrowed to a viewer — read-only, one door, page list (job #793)
- view-as pages: never offer a link that leads somewhere false
- view-as: a viewer with a hard edge, not an identity switch
- merge: a trial with no end date must not mean forever (job #796)
- platform: a trial with no end date must not mean forever (job #796)
- merge: view-as is back — see every page as any role (job #793)
- admin: the admin gate stands back while view-as is switching on
- …and 46 more

**Dropped — internal kind (docs/tests/chore/refactor) (26):**
- CI: the three reds on dev, and the toolchain break that hid them
- test(schools): teach the scope-resolver mock the list-shaped school-tag query
- test(groups): teach the mock schoolMembershipsOf's new list-shaped query
- test(schools): the delete gate, tested against a database rather than a mock
- test(security): the enrolment-repair cron enters the authenticated inventory
- test(groups): teach the mock schoolMembershipsOf's new list-shaped query
- test(schools): the delete gate, tested against a database rather than a mock
- docs(apml): belonging is the admin's act — the schools spec says so
- …and 18 more

**Dropped — nothing left after stripping jargon (6):**
- merge dev
- merge dev
- merge dev
- merge dev
- feat(schools): named seats — the admin types a name and hands over a code
- feat(schools): named seats — the admin types a name and hands over a code

**Dropped — plumbing commit (tests, mocks, notes, lockfiles) (4):**
- i18n: the nightly goes green — 34 new keys enrol, the Google door speaks, and the SW test stops pinning a retired rule
- merge: funder-export leader-access test coverage (job #607) into dev
- fix(test): index the last button rather than Array.at, which is outside this package's TS lib
- feat(canolfan): the enrolment page, the switchover setup call, and the tests Kai asked for

**Dropped — internal machinery in the claim (3):**
- merge: admin effective-access reads the player's own resolver (job #788)
- merge: admin effective-access reads the player's own resolver (job #788)
- fix(admin): show the courses an org grants through its enrolment policy

**Dropped — shipped but not switched on for users (2):**
- fix(auth): the dark Google door says so, instead of a raw Supabase 400
- feat(auth): a Google door above the email field, built and dark

<!-- /release-notes:draft-only -->
