<!-- release-notes:header -->
# Release notes — shipped 2026-09-12

Live on [saysomethingin.app](https://saysomethingin.app) since 2026-09-12. 219 commits, promoted from `staging` at `ca46ec8`.

Up to three headline features — player/learner-facing surfaces weighted first — then one
catch-all section for everything else that shipped. Deliberately under-claiming: a commit
that cannot be translated confidently into user-facing language is dropped rather than
guessed at. Generated by `tools/release-train/release-notes.mjs`.
<!-- /release-notes:header -->

## What's new

- The SW navigation fallback is the precached shell, never a runtime copy.
- Merge cs/326: the 60-day belt rewind never lands below the learner's own belt start (job #326).
- Merge cs/301: a failed class-practice fetch is loud on the classes list.

## Other stuff and bug fixes

- Merge cs/265: the class page reads the class account too; per-student cards gone (job #265).
- Merge cs/265: class rows and the class page read the class account's own progress; no per-pupil framing on a class (job #265).
- Merge cs/265 follow-up: the classes list's time in app is read for the school being viewed under View-as (job #265).
- Merge cs/266: a school leader can copy a teacher's mistaken own-account play onto the class's play-as-class account, with preview….
- Merge cs/265: practice is minutes on every school surface; the school dashboard headline is this week's in-app minutes, the same….
- Merge cs/217: a class with no pupil accounts is not 'inactive' by fiat.
- Merge cs/215: a class has started when it has practised, not when it carries a cursor.
- Merge cs/424-listening-snapshot-empty-extrapo into dev (job #424).
- Listening exercises are fetched FIRST on both paths.
- The Italian method pod is a THIRD Listening Mode slot beside Pod 1, online and offline (job #354).
- Merge origin/dev into cs/343 (#326 census doc, #340 admin nav).
- Merge cs/325: mode+belt+seed/round on every player row; Listening Mode per-clip audio_play; JP/FI rule drops unattributed rows on….
- Report a bug sheet from Settings and the learner's page (job #327).
- Every clip logs an audio_play shaped like a main-flow pod play, cycleType listening_mode; rows carry mode 'listening' (job #325).
- Every player row carries mode, belt, seedId and roundIndex in force at log time (job #325).
- Leader home lists show three rows then Show all.
- Merge cs/307: player_events orphan rows attributed from the first sync flush, env from the deployment, 157-row backfill, no env f….
- 'phrases practised this week' replaces 'phrases spoken' on every leader and teacher surface (job #306).
- Offer the clip where one exists; support audit on staging as a school leader (job #302).
- Merge cs/298: class 8H's 6 of 679 LEGOs and 53 phrases traced to the live DB; figures stand (job #298).
- Merge cs/286: schools pages open the explainer.
- The class page opens the explainer, and How this works is the louder door (job #286).
- Merge cs/267 follow-up: the org questions carry each class's in-app minutes off its class account; View-as proven not to change s….
- The class page carries the leader's copy-teacher-play card, with Handbook entry, APML and decision journal.
- Merge cs/263: the View-as button is visible in the light admin top bar (job #263).
- Support channel: door one on the dashboard, Support in the user menu, the thread view, the Handbook in place.
- Record measured miss and blob catch; add audio qualification command.
- Add scratch-only defect library and honest audio control loop.
- Offline downloads are more reliable.
- Merge cs/326: a preview-sized script cache can no longer reset a cym_s_for_eng learner past Yellow to White belt (job #326).
- The snapshot heal marks a course healed only after a successful write, so a failed first fetch retries on the next round advance….
- A payer who reopens the app offline keeps their subscription.
- The bug-report sheet stacks above the bottom nav.
- Belt-strip refs were the number 0.
- Merge cs/339: Listening Mode per-clip ReferenceError fix, belt-pip const-assign fix, audio_play seedId stamped (job #339, landed….
- #325 per-clip rows threw ReferenceError and stopped Listening Mode on staging; audio_play seedId now stamped (job #339).
- Promote dev → staging: #325 telemetry fixes (mode, belt, Listening Mode per-clip, JP/FI rule).
- The JP/FI machine rule drops only unattributed rows; a signed-in real learner is never erased by country (job #325).
- Docs+scripts(player-events): #307's Chepstow totals with SQL and fixed bounds; 2,294 was fleet-wide, Chepstow is 1,818 (job #317).
- The copy-play card never voices a failed teacher read as an empty class (job #284).
- The org questions give every class its in-app minutes off its own class account, and stop saying class time is unmeasured.
- The View-as button reads on the light admin bar instead of vanishing white-on-white.
