<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CAPACITOR_DEBUG</key>
	<string>$(CAPACITOR_DEBUG)</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>SSi (dev wrap)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- Locked-screen / background audio: without this mode iOS suspends the
	     process the moment the screen locks, killing the learning cycle. The
	     capability must ALSO be enabled on the App ID in the developer portal,
	     and an AVAudioSession(.playback) is still needed in Swift — this key
	     alone is necessary, not sufficient. UNVERIFIED on a device. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>audio</string>
	</array>
	<!-- PronunciationOverlay.vue calls getUserMedia. On iOS a microphone touch
	     without this string is not a denied permission — the OS kills the
	     process outright. The text is what the learner reads in the prompt. -->
	<key>NSMicrophoneUsageDescription</key>
	<string>SSi uses the microphone to hear you speak during practice.</string>
	<!-- APP TRANSPORT SECURITY: NOTHING TO DECLARE, deliberately. The WebView's
	     whole content comes from https://saysomethingin.app over TLS with a
	     public certificate, which is exactly the case ATS allows by default. An
	     NSAppTransportSecurity dictionary here would only ever be a hole. If a
	     field build ever needs to point at a plain-http origin, that is a change
	     to SSI_SHELL_ORIGIN and it will fail loudly rather than silently — which
	     is the right way round.

	     No NSSpeechRecognitionUsageDescription either: the app reaches for
	     getUserMedia in PronunciationOverlay.vue and never for the Web Speech
	     API, so the key would describe a capability we do not use. -->
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
    <false/>
    <key>UISceneConfigurations</key>
    <dict>
      <key>UIWindowSceneSessionRoleApplication</key>
      <array>
        <dict>
            <key>UISceneConfigurationName</key>
            <string>Default Configuration</string>
            <key>UISceneDelegateClassName</key>
            <string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
            <key>UISceneStoryboardFile</key>
            <string>Main</string>
        </dict>
      </array>
    </dict>
	</dict>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<!-- arm64, not the template's armv7. armv7 is 32-bit and no device that can
	     run this build has had a 32-bit processor for a decade — the deployment
	     target is iOS 15, which is arm64 only. The template value was never a
	     working filter, just an inaccurate one. -->
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
</dict>
</plist>
